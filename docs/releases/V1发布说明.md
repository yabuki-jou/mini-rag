# V1 候选发布说明

> 状态：候选发布说明，尚未 push/tag。

## 代码基线

功能代码基线为 `af5271e`。当前没有 `v1.0.0` tag；本说明只整理发布候选状态，不代表已经发布。

## 已纳入范围

- 智慧档案 V1：项目隔离、文档上传解析、字段草稿与确认、正式索引、清单关联、检索问答、物理删除和脱敏审计。
- 制度 Agent：Bearer 认证、知识库范围注入、只读制度检索、会话消息恢复和工具调用审计。
- 认证：Argon2 密码哈希、Access/Refresh Token、刷新不轮换和可撤销注销。
- 评测：制度 Agent 与智慧档案评测已分别迁入 `evals/policy_agent/`、`evals/archive/`；`pixie_qa/` 不再承载评测源码。
- 结构收口：services 按业务域分包，旧 RAG Prompt、Agent 会话消息/审计/执行职责已归位，测试目录同步镜像。

## 验证证据

- 后端完整回归：`487 passed, 2 skipped, 130 warnings`；services 定向回归：`138 passed, 1 skipped`。
- 相邻 Vue 工作台历史最终证据：`122 passed`，`typecheck` 和 `build` 通过。
- AV1-P02/D6-B 真实固定质量门：有据正确回答并引用 `7/8`、无据拒答 `2/2`、项目隔离拒答 `2/2`。
- 编译检查和 `git diff --check` 已通过。

## 限制与未验证边界

- Q-01 仍存在有直接证据时的安全漏答；不能据此宣称所有有据问题均通过。
- 独立 `POSTGRES_TEST_URL` 未配置；专用 PostgreSQL 自动化迁移测试仍跳过。
- 本轮没有新增浏览器 DOM 点击证据，也未新增 DOM 契约。
- 未验证云部署、容量、长时间稳定性、多实例、生产备份恢复和完整云端资源拓扑。
- PostgreSQL、Chroma 与文件系统之间不是原子分布式事务；已有故障恢复证据不等于穷尽所有故障组合。

## 待用户动作

1. Review 当前 6 个未推送功能提交及本次发布文档提交。
2. 决定是否 push 并创建 `v1.0.0` tag。
3. 也可不打 tag，直接将当前基线和验证证据整理为作品集材料；云端部署与 Q-01 后续优化另行决策。

详细验收证据见[验收报告](../implementation/验收报告.md)，当前状态见[交接文档](../stage/handoff.md)。
