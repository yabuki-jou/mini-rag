# Agent 逻辑导览

> **运行时说明（2026-08-07）：** 制度检索代码已改为使用 Chroma HTTP 客户端。它通过服务端
> 注入的 `user_id + kb_id` 执行 metadata 过滤；本地 Chroma 迁移与健康连通已验证。旧制度
> Agent 的向量重建和质量回归仍须用其独立资料验证。云端部署是否进行以及具体方案另行确认，详见
> `docs/design/Chroma迁移决策.md`。

当前 Agent 是单图、只读的企业知识库检索 Agent。

> 本文只描述当前已实现的制度检索 Agent 基座。智慧档案 V1 已完成 Parser 规则冻结、
> 虚构验收资料、数据库/模型/公共授权基础、P04 前置模型分层、项目 CRUD/模板复制 API、
> 清单项 CRUD/派生状态 API、P05 项目内上传、P06 正式解析/快照/重试、P07 手工草稿/字段检查、P08 AI 建议/安全 regenerate、P09 确认/INDEX/取消确认、P10 清单关联/目录/审计、P11 正式检索、P12 证据问答和 P13 物理删除；P14-D6-B 固定质量门、P13 真实恢复、企业扩展集和 FR-034～FR-041 Vue 分阶段真实代理链路已通过。它不复用本 Agent 作为归档状态或档案问答的实现依据。

> **档案 V1 历史状态（2026-08-28）：** P09 已完成真实确认—INDEX 路由链路和取消确认清理；P10～P13
> 已实现清单关联/目录/审计、正式检索、证据问答和物理删除切片。P14 固定集显示当前纯稠密检索不能
> 同时满足有据 `7/8` 与无据拒答 `2/2`，真实 DeepSeek、P13 真实跨存储恢复和完整端到端验收仍待完成。
> 本 Agent 仍只负责制度检索，不承担档案业务状态。

> **档案 V1 当前状态（2026-09-11）：** AV1-P01～P14 本地功能与验收收口已完成；准确范围、
> 未验证边界和下一步以 `docs/implementation/验收报告.md` 与 `docs/stage/handoff.md` 为准。

| 层 | 文件 | 职责 |
|---|---|---|
| HTTP | `app/routers/agent.py`, `app/schemas/agent.py` | 会话、消息、历史、审计契约 |
| 应用服务 | `app/services/agent/sessions.py`、`messages.py`、`audit.py`、`execution.py` | 会话范围、Checkpoint 消息、脱敏审计和 Graph 执行 |
| Graph | `app/agents/admin/graph.py` | 上下文校验、模型与 ToolNode 闭环 |
| Runtime | `app/agents/admin/runtime.py` | SQLite Checkpoint 生命周期 |
| Tool | `app/agents/tools/policy_tools.py` | 使用注入范围调用 RAG 检索 |
| 存储 | PostgreSQL、Chroma、Checkpoint SQLite | 业务事实、向量索引、Graph 状态 |

调用过程：服务端从 `AgentSession` 注入 `user_id` 和 `kb_id` → Graph 校验
UUID → DeepSeek 决定是否调用 `search_company_policy(query)` → Tool 使用固定范围
检索 Chroma → 模型根据真实结果回答 → 应用服务返回引用并保存脱敏工具日志。

模型只能生成 `query`，不能生成或覆盖身份。工具日志仅记录是否提供查询、
查询长度、是否命中、结果数量、耗时和错误码，不保存制度正文与内部异常。

PostgreSQL 保存 Agent 会话和审计；Checkpoint SQLite 保存消息与授权范围。两者
不建立外键。当前没有写工具、`interrupt/resume` 或人工决定接口。
