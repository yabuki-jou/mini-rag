# 智慧档案与企业文档智能 V1 实施计划

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 业务方向 | 智慧档案与企业文档智能 |
| V1 项目名称 | 工程项目资料智能归档与检索工作台 |
| 文档类型 | 实施计划（不含实现代码） |
| 依据 | `docs/design/需求说明.md`、`docs/design/技术架构.md`、`docs/design/数据库设计.md`、`docs/design/接口设计.md` |
| 验证门槛 | `docs/review/验证冻结清单.md`（v3） |
| 状态 | AV1-P01～P14 的本地功能与验收收口已完成；D6-B 固定质量门、正式 Embedding/Collection 切换、P13 真实恢复、企业扩展集及 FR-034～FR-041 Vue 分阶段真实代理链路均已完成。未验证边界见验收报告 |
| 更新日期 | 2026-09-11 |

> **历史实现覆盖（2026-08-28）：** P09 真实确认—INDEX 路由链路、真实 Chroma/BGE canary 与取消确认
> 清理已完成；P10～P13 的实现切片和确定性测试已完成。P11 已记录单文档检索 P95 约 `125.97 ms`，
> P14 固定集复验中三种纯稠密表示均保持项目隔离 `2/2`，但无据拒答 `2/2` 时有据召回仅为
> `2/8`、`5/8`、`6/8`，不能冻结阈值。随后阶段 C 本地 Reranker 固定集的完整候选为有据 `7/8`、无据拒答 `0/2`、隔离 `2/2`；拒绝全部无据候选时有据降为 `3/8`，仍不能冻结阈值；P12 DeepSeek、P13 真实恢复和完整 Vue 验收仍归入 P14。
> 问题分析、证据边界和候选方向见 `docs/review/P14-rag检索质量改进/检索质量问题分析与改进策略.md`；后续 P14
> 检索质量修改必须遵循 `docs/review/P14-rag检索质量改进/P14-C2-检索质量优化方案.md`。
> 2026-08-31 已完成阶段 A 的诊断代码、回归和真实固定集复验：标准证据均进入前 3 候选，但无据拒答
> `2/2` 时有据仅为 `5/8`，未发现范围泄露；下一步按实施方案进入阶段 B，阈值仍不得冻结。

本计划只安排需求编号 `FR-030`～`FR-041` 的智慧档案 V1 工作。它不替代
`docs/implementation/既有检索与智能体实施计划.md` 中已经完成的既有 RAG 与 Agent 基座记录，也不把计划中的
模块、接口或测试描述为已经存在。

## 2. 实施原则与边界

### 2.1 执行顺序

```text
智慧档案 V1 实施顺序（当前已完成 P01～P14 本地功能与验收收口）
→ AV1-P01 Parser 可行性验证与规则冻结
→ AV1-P02 虚构验收资料与人工标注
→ AV1-P03 数据库与模型基础
→ AV1-C01 历史 Chroma 独立实验（已完成）
→ AV1-C02 Chroma 向量后端本地迁移（已完成）
→ AV1-A01 账号密码认证与 Bearer 身份切换（已完成隔离验证）
→ AV1-P04.2 清单项 API 与派生状态（已完成）
→ AV1-P05 上传（已完成）
→ AV1-P06 正式解析（已完成）
→ AV1-P07 手工草稿与字段检查
→ AV1-P08 AI 建议与安全重新生成（已完成）
→ AV1-P09 确认入档与 Final Collection
→ AV1-P10 清单关联、目录与审计
→ AV1-P11 正式检索与阈值标定
→ AV1-P12 带证据问答
→ AV1-P13 删除与跨存储恢复
→ AV1-P14 集成验收与文档交付
```

按学习规则，进入编码后一次只执行一个任务；每个任务验证完成并由学习者确认后，才进入
下一任务。`AV1-P02` 与 `AV1-P01` 没有代码依赖，但为保持单步骤学习节奏，先完成
`AV1-P01`，再准备完整验收资料。

### 2.2 不可跨越的门槛

| 门槛 | 对应计划任务 | 在冻结前允许做什么 | 在冻结前不得声称完成什么 |
|---|---|---|---|
| 1.1 Parser 规则 | AV1-P01 | 编写隔离的可行性验证代码与样本 | 正式 Alembic 迁移、解析归档 API、Parser 验收测试 |
| 1.0 验收资料 | AV1-P02 | 继续业务基础实现 | 检索/问答质量验收、阈值标定 |
| C01 Chroma 独立验证 | AV1-C01 | 记录候选云主机的 Chroma 独立实验，不删除 Milvus 数据 | 历史实验可追溯，但不构成部署批准 |
| 2.1 BGE/Chroma | AV1-P09 | 完成 PostgreSQL、手工归档路径和 AV1-C02 | FR-039 正式检索 API、检索验收 |
| 2.2 检索阈值 | AV1-P11 | 实现检索纵向切片并记录候选分数 | 将问答接口视为完成或验收通过 |
| 3.1 跨存储恢复 | AV1-P13 | 实现确认、删除的正常路径 | FR-036、FR-040 的集成验收 |
| 3.2 回答引用 | AV1-P12 | 实现问答切片 | FR-039 问答集成验收 |

门槛的完整定义与冻结记录以
`docs/review/验证冻结清单.md` 为准；本节只说明计划依赖，不能替代该清单。

### 2.3 共同工程约束

- 保持 `Router → Application Service → Domain Service → 存储/外部依赖` 分层；不增加空转 Repository 层。
- 每个受保护接口从已验证 Bearer Access Token 建立当前用户与 `ProjectContext`；客户端和模型不能
  提交或覆盖所有者、项目/知识库范围，也不再接受 `X-User-ID`。密码、Refresh Token 明文和
  Token 负载不得写入审计或日志。
- PostgreSQL 是业务事实来源，原文件与解析快照位于文件系统，Chroma 只保存可重建的正式 Chunk；
  Compose 内 API 经内部 `chroma:8000` 访问。仅本地开发时可额外映射 `127.0.0.1:8001` 供宿主机
  Python 调试，局域网和公网不可访问。云端是否部署及其端口策略暂定至 V1 完成后。
- 未确认、待重新确认及 `visibility_blocking` 删除中的档案，均不得进入正式目录、检索或问答。
- 普通自动测试不调用真实 DeepSeek；外部模型只使用虚构或脱敏资料。旧 RAG/Agent 测试必须继续通过或明确说明其环境依赖。
- 不实现标书投标、OCR、Excel、跨项目检索、公开多用户前端、多角色、混合检索、任务队列或知识图谱。P14 已确认的唯一例外是本地 Reranker 对已授权且范围校验通过的 Chroma Top-30 候选重排；不引入 BM25、远程排序服务或扩大候选来源。相邻 Vue 工作台仍仅用于本地联调，必须通过 FastAPI 与 Bearer/项目授权边界。

## 3. 任务总览

| ID | 需求 | 依赖 | 目标 | 完成判定 |
|---|---|---|---|---|
| AV1-P01 | FR-033、NFR-016 | 无 | 验证 Parser 的定位、归一化和有效文本规则 | 门槛 1.1 已冻结并写入 Parser 说明 |
| AV1-P02 | NFR-017、质量验收 §12 | 无（建议在 P01 后） | 准备虚构样本、人工标注和固定问题集 | 门槛 1.0 资料与 Ground Truth 可复现 |
| AV1-P03 | FR-030～041 共用 | P01 | 建立 V1 数据库模型、迁移和公共授权/错误基础 | 空库前向迁移与约束测试通过 |
| AV1-P04 | FR-030、FR-031 | P03 | 项目、演示清单、版本控制与所有权 | 项目/清单 API 正常、冲突、越权测试通过 |
| AV1-A01 | FR-019～FR-021 | P03 | 账号密码、JWT 会话、Bearer 认证与旧身份切换 | 注册、登录、刷新、注销、越权与旧 `X-User-ID` 拒绝测试通过 |
| AV1-C01 | NFR-015、历史部署实验 | 无 | 在候选 2 vCPU / 2 GB 环境验证 Chroma 内部网络、重启持久化、隔离、删除与基础资源 | 历史实验已记录，不构成部署批准 |
| AV1-C02 | 既有 RAG 基座、NFR-015 | C01 | 将现有 VectorService 及本地 Compose 从 Milvus 替换为 Chroma，并完成本地迁移验证 | 代码、单测、命名空间、隔离/删除与 Docker 健康验证通过；云端完整栈另行决定 |
| AV1-P05 | FR-032 | P03、P04 | 项目内上传、容量与原文件哈希去重 | 文件保存与重复/上限/隔离测试通过 |
| AV1-P06 | FR-033 | P01、P03、P05 | 将冻结后的 Parser 接入文档状态、快照与重试 | 四格式定位、扫描件失败、幂等测试通过 |
| AV1-P07 | FR-035 | P03、P06 | 人工草稿、字段证据、检查和重新确认入口 | 无 AI 的完整手工确认前置条件可验证 |
| AV1-P08 | FR-034 | P06、P07 | DeepSeek 分类、字段与证据建议及安全重生成 | mock 测试覆盖成功、失败、人工覆盖保护 |
| AV1-P09 | FR-036 | P03、P06、P07 | 确认入档、Final Collection 与可见性闸门 | 门槛 2.1 纵向链路与索引基线已记录 |
| AV1-P10 | FR-037、FR-038、FR-041 | P04、P07、P09 | 人工关联、缺失派生、双列表和脱敏审计 | 关联、筛选、目录隔离、审计 API 测试通过 |
| AV1-P11 | FR-039（检索） | P02、P09 | 正式检索与质量门验证 | D6-B 固定集和企业扩展集通过；Q-01 安全漏答保留为限制 |
| AV1-P12 | FR-039（问答） | P11 | 仅基于正式证据的 DeepSeek 问答 | 文档绑定、拒答、Top-8 与真实模型质量门通过；正式配置已切换 |
| AV1-P13 | FR-040；FR-036/040 恢复 | P05、P09、P10 | 物理删除、可见性阻断与可重入恢复 | 确定性故障注入与真实跨存储恢复均通过；故障删除、可见性阻断、文件保留、健康服务重试、三层归零和单条审计均已核验 |
| AV1-P14 | FR-030～041 | P02～P13 | 全链路验收、README/报告同步 | 验收结论、未验证项和演示步骤可追溯 |

## 4. 分任务计划

### AV1-P01 Parser 可行性验证与规则冻结（已完成，2026-08-06）

**目标与范围**

- 验证 PDF 页码、DOCX 段落序号、TXT/MD 行号范围均从 1 开始；DOCX 同时产生规范化段落文本前 50 字的辅助定位。
- 已固定 UTF-8 解码、换行归一化、空白归一化、表格/编号列表处理、有效文本最小字符阈值与最大解析文本量，详见 `docs/design/智慧档案V1解析器设计.md`。
- 使用同一原文件重复解析，确认定位和快照哈希可重复生成；扫描 PDF 或空壳文件必须得到 `PARSE_FAILED` 对应的稳定失败分类。

**明确不做**

- 不创建 `projects`、`archive_documents`、`parsed_snapshots` 等正式迁移或模型。
- 不注册归档 Router，不调用 DeepSeek/Chroma，不改写现有 `documents` 业务状态。

**预期改动范围（执行时再确认）**

- `app/services/archive/parser.py`（归档专用隔离适配器，未接入旧 RAG Parser）；
- `tests/services/archive/test_parser.py`（运行时动态生成虚构样本）；
- `docs/design/智慧档案V1解析器设计.md`（冻结规则与实际结果）。

**验证与完成条件**

1. 固定样本的 PDF/DOCX/TXT/MD 各至少一份解析成功；定位类型和值满足需求 §7.6。
2. 同一文件重复执行的 `ParsedFragment` 顺序、定位和 `snapshot_hash` 一致。
3. 无可提取文本 PDF 返回扫描件/无文本的受控失败分类，不被误判为成功。
4. 空壳文本被有效文本阈值过滤；阈值和规范化规则写入 Parser 设计说明。
5. 本任务相关测试和 `python -m compileall -q app tests` 通过；冻结结论同步到验证清单引用的位置。

### AV1-P02 虚构验收资料与人工标注（已完成，2026-08-06）

**目标与范围**

- 形成 12～18 份虚构资料，覆盖六个资料类型以及 PDF、DOCX、TXT、MD；增加 2～3 份异常样本。
- 为每份资料标注类型、可识别字段和标准证据；准备 8 个有据、2 个无据、2 个隔离固定问题。
- 样本与标注不含真实商业秘密、个人敏感信息或未获授权资料。

**完成条件**

- 生成器和标注格式受版本控制；生成的本地资料位于 `tests/pytest_docs/` 并由 Git
  忽略，目录缺失时测试自动调用生成器重建。
- 其仅作为质量评测 Ground Truth；不作为真实工程归档规则，也不直接决定模型 Prompt。

**完成结果（AV1-P02，2026-08-06）**

- 已生成 12 份正常虚构资料（六类资料各 2 份，覆盖 PDF、DOCX、TXT、MD）和 3 份
  异常资料（字段缺失/分类混淆、同项目原文件哈希重复、无可提取文本的扫描 PDF）。
- 已生成字段与证据 Ground Truth、12 个固定问题（8 有据、2 无据、2 项目隔离）及本地
  查看说明；`tests/services/archive/test_evaluation_data.py` 验证资料、证据、重复哈希和问题集契约。
- 本任务未调用 DeepSeek，也未执行 Chroma 检索、阈值标定或问答质量评分；这些结果仍分别由
  AV1-P11、AV1-P12 与 AV1-P14 产生。

### AV1-P03 数据库、模型与公共授权基础（已完成，2026-08-06）

**目标与范围**

- 在 `0004_remove_leave_domain` 后创建前向 `0005_archive_v1_schema`，实现 `projects`、归档文档/快照/字段/证据、清单/关联、操作/审计等已确认结构和约束。
- 依数据库设计将 `documents.content_hash` 演进为原文件 `file_hash`，建立项目关系、容量计数、同项目哈希唯一性和与知识库的一对一范围约束；不把旧文档自动转换为档案。
- 新增 `ProjectContext` 所有权依赖、归档枚举、稳定错误码基础和最小 Schema/Model；保留现有 Agent、聊天和旧知识库行为。

**测试与完成条件**

- 空 PostgreSQL 前向迁移、迁移 SQL 检查和数据库约束测试通过；失败时不使用 `alembic stamp` 掩盖问题。
- 覆盖同用户项目名、同项目 `file_hash`、每文档七字段/当前快照、运行中操作、`visibility_blocking` 的数据库约束。
- 既有迁移与回归测试仍可运行；在线 PostgreSQL 若环境不可用，必须单列为未验证项。

### AV1-A01 账号密码认证、JWT 会话与身份切换（已完成隔离验证，FR-019～FR-021）

**目标与范围**

- 新增公开注册、登录、刷新和注销接口；使用全局唯一小写 `username` 登录，保留 `name` 作为显示名。
- 使用 Argon2 密码哈希与带 `sub`、`sid`、用途、签发/过期时间的 JWT；Access Token 30 分钟，
  Refresh Token 7 天。
- 增加 `auth_sessions` 表，只保存 Refresh Token 哈希、过期时间和撤销状态；刷新只签发新的
  Access Token，不轮换 Refresh Token。
- 将 `CurrentUserDep` 从 `X-User-ID` 完全切换为 Bearer Access Token，移除公开 `/users` 模拟身份入口。
- 旧用户保留业务归属数据但无凭据不能登录；提供不打印密码、不列举用户的本地初始化命令，按
  明确的用户 ID 设置用户名和密码。

**验证重点**

- 注册用户名规范化冲突、弱密码拒绝、密码哈希不回显，以及用户名不存在和密码错误的响应一致。
- 登录、刷新、注销、过期、伪造、用途错误和已撤销 Refresh Token 均返回稳定结果。
- 所有旧知识库、Agent、项目和聊天 API 均改用 Bearer Access Token；仅携带或伪造
  `X-User-ID` 必须不能通过身份校验。
- PostgreSQL 迁移前向兼容旧用户；迁移、模型约束、路由和服务测试不读取真实 `.env` 或数据库数据。

**实际验证与未验证项**

- 已通过隔离 SQLite 的注册、登录、刷新、注销、用途错误、旧 `X-User-ID` 拒绝、旧用户拒绝登录、
  会话撤销和既有受保护路由回归测试。
- revision `0010_account_auth` 已通过 SQLite 前向迁移和注释契约测试，并已在当前 PostgreSQL
  开发库实际前向迁移；已核对认证表、字段、索引及 Refresh Token 哈希唯一约束。

### AV1-P04 项目与项目清单

**共同边界**

- 暂不实现档案—清单关联（AV1-P10）。
- 删除空项目时不级联删除其绑定的 `knowledge_bases` 记录。该记录可能仍在旧
  `/knowledge-bases` 接口中可见；旧系统不感知项目创建/删除生命周期，后续如需清理须由
  独立运维任务处理，不能在项目删除接口中删除可能承载旧文档的数据。

#### AV1-P04.1 项目 CRUD、内部知识库范围与模板复制（已完成，FR-030）

**已完成范围**

- 实现项目创建、读取、修改、空项目删除和 Bearer 身份后的所有权校验。
- 创建项目时可选择复制唯一的五项虚构演示清单；不提供用户自定义模板的创建、保存或复用。

**已验证重点**

- 同用户去首尾空格后的重名冲突、跨用户可同名、越权不泄露、非空项目禁止删除。
- 模板复制互不影响；修改关键匹配字段时，后续关联会失效的规则由 AV1-P10 覆盖。

**完成结果（P04.1 FR-030，2026-08-06）**

- 已实现 `POST/GET/PATCH/DELETE /projects`、项目专属内部知识库范围、同用户去空白重名冲突、
  乐观锁、所有权校验、分页列表与非空项目删除拒绝。
- `use_demo_checklist=true` 会在创建事务中复制五项虚构演示清单；空项目删除清理项目级清单，
  但保留内部 `knowledge_bases` 记录，防止影响旧 RAG/Agent 的文档数据。
- `tests/routers/test_projects.py` 覆盖模板复制、冲突、隔离、版本、越权和删除边界。

#### AV1-P04.2 清单项 API、版本控制与派生状态（已完成，FR-031）

**目标与范围**

- 提供 `GET/POST/PATCH/DELETE /projects/{project_id}/checklist-items`，只允许项目所有者访问和维护项目独立清单项。
- `POST` 创建清单项必须校验 `expected_project_version`；在同一事务中创建清单项、写入脱敏审计并使 `projects.version` 原子加一。清单项修改使用其 `expected_version` 防止覆盖新数据；旧版本均返回 `409 VERSION_CONFLICT`。
- 清单列表实时派生 `SATISFIED`、`MISSING`、`NOT_PROVIDED` 和 `confirmed_document_count`。只有同项目 `CONFIRMED` 档案与 `CONFIRMED` 关联同时存在才为 `SATISFIED`；必需项无满足关联为 `MISSING`，可选项无满足关联为 `NOT_PROVIDED`。不得仅按资料类型或项目阶段自动判定满足。
- 修改清单项的名称、资料类型或项目阶段时，使已有确认关联失效并重算状态；仅修改说明不影响关联。删除清单项时删除其关联并写入脱敏审计。
- 本任务只实现清单项维护和读取已有确认关联后的派生结果；不提供档案—清单项关联建议、创建或删除接口，该部分仍由 AV1-P10（FR-037）实现。

**验证重点**

- 模板复制后的项目清单可以独立增删改，跨项目或跨用户不能读取、修改或删除。
- 创建请求的旧项目版本、修改请求的旧清单项版本均返回稳定的 `409 VERSION_CONFLICT`，且不产生重复清单项或审计成功记录。
- 当前尚无已确认关联时，必需项返回 `MISSING`、可选项返回 `NOT_PROVIDED`；后续存在已确认档案和已确认关联时才返回 `SATISFIED`。
- 修改名称、资料类型或项目阶段会使已存在的确认关联失效；只改说明不会失效。删除清单项后不再出现在列表，且其关联被清理。

**完成结果（P04.2 FR-031，2026-08-12）**

- 已实现 `GET/POST/PATCH/DELETE /projects/{project_id}/checklist-items`。所有接口先由
  `ProjectContext` 校验项目所有权；创建以 `expected_project_version` 原子递增项目版本，修改以
  `expected_version` 防止陈旧覆盖，关键匹配字段改变时将确认关联更新为 `INVALIDATED`。
- 列表只基于同项目 `ArchiveDocument.CONFIRMED` 与 `ChecklistLink.CONFIRMED` 派生
  `SATISFIED`、`MISSING`、`NOT_PROVIDED` 和确认档案计数；资料类型或项目阶段不会自动满足清单。
- 清单项增删改在同一事务写入脱敏审计；删除显式清理关联但保留删除审计。关联建议、创建和
  删除接口仍属于 AV1-P10，P04.2 测试仅为验证读取派生结果而直接准备确认档案/关联事实。
- `tests/schemas/test_checklist_schemas.py`、`tests/services/project/test_checklists.py` 与
  `tests/routers/test_projects_checklist.py` 分别覆盖 Schema 边界、服务层陈旧项目版本的
  事务回滚，以及 API 的派生状态、项目/清单版本冲突、越权、关联失效、删除和审计；测试目录
  与 `app/schemas/`、`app/services/`、`app/routers/` 一一对应。隔离 SQLite/TestClient 已验证，
  真实 PostgreSQL 并发竞争尚未验证。

### AV1-C01 Chroma 独立可行性验证（已完成，2026-08-07）

**目标与范围**

- 在目标 2 vCPU / 2 GB 云环境，以 Chroma 单机服务替代 `etcd + MinIO + Milvus`；Chroma
  只加入内部网络和持久化卷，不映射宿主机端口。
- 使用虚构资料验证 Chroma 内网心跳、容器重启持久化、`user_id + kb_id` 隔离和精确删除；
  记录 Chroma 空闲内存与主机可用内存，不得删除或修改
  现有 Milvus 数据。

**验证重点**

- 已在目标环境通过内部网络、过滤、精确删除、容器重启持久化和空闲资源检查；实测记录见
  `docs/design/Chroma迁移决策.md`。C01 执行时当前代码仍依赖 Milvus，因此本任务不能验证完整应用栈。
- FastAPI、PostgreSQL、Chroma 与本地 BGE 的完整栈 OOM/重启/健康验证改为 AV1-C02 的
  完成条件；若未通过，要求用户选择增加内存、迁出 PostgreSQL 或拆分 Embedding 服务。
- 仅 FastAPI 对外发布 HTTP(S)；直接访问 Chroma 的宿主机端口必须不可用。

### AV1-C02 Chroma 向量后端迁移（本地验证已完成，2026-08-07）

**目标与范围**

- 移除运行时代码、依赖和 Compose 对 Milvus、etcd、MinIO 的依赖，改用 Chroma HTTP 客户端、
  持久化服务、健康检查和受控错误映射；锁定 Chroma 镜像版本或 digest。
- 重写 `vector_service`、旧制度检索、文档删除和健康检查的向量调用，保持既有 HTTP API、
  `user_id + kb_id + document_id` 服务端范围过滤与脱敏错误契约。
- 不迁移历史向量；仅在 Chroma 测试通过后，由原文件和业务记录重建虚构/可授权资料的向量。
- 用 AV1-P02 验收集重新标定距离度量、分数方向和阈值；不得复用旧 Milvus COSINE 阈值。
- 本机验证 FastAPI、PostgreSQL、Chroma 和本地 BGE 的连通与健康状态；目标云环境的完整栈资源、
  向量化、检索、OOM/重启验证暂定至 V1 完成后的部署决策，不得以 C01 的 Chroma 空闲内存替代。

**验证重点**

- 旧制度检索的写入、检索、引用、隔离和删除回归通过；Chroma 断连只返回稳定
  `VECTOR_UNAVAILABLE`，不泄露内部地址或异常。
- 本机 Chroma 不对局域网或公网暴露，重建过程不产生跨用户/知识库数据泄露；云端完整栈的
  内存、OOM、重启与可恢复健康检查属于后续部署验收，不是当前开发完成条件。

### AV1-P05 项目内上传与重复校验

**目标与范围**

- 实现只接收 PDF/DOCX/TXT/MD 的 multipart 上传；在文件落盘前完成所有权、20 MB、100 份现存文档和同项目 `file_hash` 校验。
- 创建原文件和 `UPLOADED` 归档记录；上传不得自动解析、调用模型或写 Chroma。

**验证重点**

- 同项目重复返回当前项目既有文档摘要且无新文件/记录；不同项目相同文件可上传且不泄露信息。
- 容量并发下以项目行锁/原子计数防止突破 100；超限不产生半成品。

### AV1-P06 正式解析、快照与重试

**目标与范围**

- 将 AV1-P01 冻结的解析规则接入 `UPLOADED → PARSED` 与 `PARSE_FAILED → PARSED` 专用重试流程。
- 保存不可变 `ParsedSnapshot`、`file_hash` 与 `snapshot_hash`，记录受控失败摘要；普通 parse 的错误状态和重复调用均保持幂等。

**验证重点**

- 四格式的定位、扫描 PDF 提示、空文本阈值、解析重试、快照不可替换及旧状态操作拒绝。
- 解析不写正式向量、不生成字段草稿、不递增草稿版本。

### AV1-P07 手工草稿、字段证据与人工检查

**目标与范围**

- 实现 `manual-draft`、草稿读取、七字段更新、证据保存、检查状态、乐观锁，以及已确认数据修改后的 `PENDING_RECONFIRMATION`。
- 先建立不依赖模型的完整人工归档路径：标题和资料类型必须非空，其他字段可明确接受为空；人工无证据值必须显式标记。

**验证重点**

- `PARSED/SUGGESTION_FAILED → PENDING_CONFIRMATION` 的空白草稿入口；未开始草稿不得编辑。
- 七字段仍有待检查、标题/类型为空、版本陈旧、伪造 AI 来源均被拒绝。
- 已确认档案修改字段或证据后立即退出正式范围；此时尚未要求其已实现正式索引。

**完成结果（2026-08-26）**

- 新增 `POST .../manual-draft`、`GET .../draft` 和 `PUT .../fields/{field_name}`，覆盖
  `PARSED/SUGGESTION_FAILED → PENDING_CONFIRMATION`、七字段空白草稿、当前快照证据、
  人工无证据标记、来源伪造拒绝、值列与检查状态规则、乐观锁和
  `CONFIRMED → PENDING_RECONFIRMATION`。
- P07 只实现不依赖模型的手工路径，不包含 P08 的 DeepSeek 建议与安全重生成。
- P07 相关路由/服务测试通过；默认全量回归为 `164 passed, 2 skipped, 16 warnings`，
  `compileall` 通过。

### AV1-P08 AI 建议与安全重新生成

**目标与范围**

- 实现首次建议、失败后重试、未人工编辑草稿的重新生成；模型只能使用当前文档快照和固定字典。
- 校验 AI 非空字段均有当前快照证据；模型不可用与无效输出使用稳定错误码。

**验证重点**

- 单元/API 测试使用 fake/mock DeepSeek，覆盖成功、连接/超时一次重试、格式错误、无字段依据、手工草稿降级。
- 重新生成在模型调用后再次校验版本与人工编辑状态，不能静默覆盖人工字段或人工证据；失败时保留原草稿。
- 实际 AI 准确率不在本任务宣称达标，留给 AV1-P14 的固定验收集。

**完成结果（2026-08-26）**

- 已实现首次 `suggestions`、失败后的 `suggestions/retry`，以及携带
  `expected_version` 的安全 `suggestions/regenerate`；AI 非空字段必须绑定当前解析快照证据。
- 连接/超时只自动重试一次；无效输出进入 `SUGGESTION_FAILED`，regenerate 失败保留原草稿；
  人工字段、人工证据或版本变化均拒绝覆盖，并成功写入脱敏 `SUGGESTION_REGENERATED` 审计。
- P08 相关路由/服务测试覆盖成功、失败、重试、格式错误、人工覆盖和模型调用期间版本竞争；
  全量回归为 `173 passed, 2 skipped, 16 warnings`，`compileall` 通过。fake 模型不代表真实
  DeepSeek 事实性或引用质量，固定资料评估留给 AV1-P14。

### AV1-P09 正式确认、Final Collection 与索引纵向切片

**目标与范围**

- 实现确认、取消确认、重复确认幂等、`ArchiveOperation(INDEX)` 和正式可见性闸门。
- 在独立 `archive_final_chunks` Collection 中写入确认后的原文 Chunk；不复用既有制度检索 Collection。
- 运行门槛 2.1：使用实际 BGE/Chroma 确认向量维度、Collection 距离配置、`confirmed_document_ids ≤ 100` 的 `where` 过滤，以及本地 P95 检索延迟记录。

**验证重点**

- 仅所有字段检查完成且必填字段非空时可确认；成功后写一次审计并进入正式范围。
- 确认失败不能假装成功；取消确认和字段修改均保守移出正式范围并清理/阻断 Final Chunk。
- 2.1 的性能值只记录为本地演示基线，不用实测值反向制造通过阈值。

**当前切片完成结果（2026-08-26）**

- 已实现 `POST .../confirm` 的人工确认前置条件：七字段均已检查、标题与资料类型非空，
  AI 非空字段必须有当前快照证据，并使用 `expected_version` 做乐观锁。
- 前置条件通过后写入 `CONFIRMED`、确认人/时间、当前快照指纹和脱敏
  `ARCHIVE_CONFIRMED` 审计；当前切片不写 Chroma Final Collection、不生成 Final Chunk，
  `final_chunk_count` 保持为 0，正式索引仍待后续切片完成。
- P09 相关测试 `25 passed`；全量回归 `178 passed, 2 skipped, 16 warnings`，并通过
  `compileall`。重复确认在当前版本下幂等且不重复写成功审计。

**第二个基础切片完成结果（2026-08-26）**

- 已从当前解析快照构建稳定 Final Chunk：校验文档归属、快照哈希、Parser 版本、片段定位，
  并生成可重复的 Chunk ID。
- 已新增独立 `archive_final_chunks` Collection 适配器，固定 cosine 配置；写入契约包含
  `user_id`、`project_id`、`kb_id`、`document_id`、快照哈希、Parser 版本和位置元数据。
- 相关服务/配置测试 `13 passed`；全量回归为 `183 passed, 2 skipped, 16 warnings`，并通过
  `compileall`。本切片只验证 Mock Chroma 契约，未执行真实 Chroma 写入，也未接入 `INDEX`
  事务和确认路由。

**第三个基础切片完成结果（2026-08-26）**

- 已新增 `archive_index_service.py` 内部服务：为 `CONFIRMED` 文档创建/重试
  `ArchiveOperation(INDEX)`，拒绝同文档其他运行中操作，调用 Final Chunk/Embedding/Collection
  适配器，并在成功时回写快照哈希和 Chunk 数量。
- 外部失败会将同一操作记录为 `FAILED`，保存稳定错误码/摘要，保留后续重试入口；同快照已有成功
  索引时幂等复用，不重复调用向量写入。相关 P09 测试 `27 passed`；当前全量回归为
  `188 passed, 2 skipped, 16 warnings`，并通过 `compileall`。
- 本切片仍只使用 Mock Chroma/Embedding；未执行真实外部写入，未把确认路由接入 INDEX，也未实现
  跨存储补偿、取消确认、正式目录或正式检索。

**第四个基础切片完成结果（2026-08-27）**

- 确认服务在确认事务提交并刷新状态后调用内部 `index_confirmed_document`；INDEX 的稳定
  `AppError` 会透传到确认接口；已有 Final Chunk 的 `CONFIRMED` 幂等确认不会重复触发索引，
  未完成索引的同版本确认可以进入恢复重试。
- 确认路由测试使用默认 INDEX stub 隔离外部服务，并覆盖成功调用链和向量依赖失败；相关 P09 测试
  `30 passed`，当前全量回归为 `191 passed, 2 skipped, 16 warnings`，并通过 `compileall`。
- 本切片未执行真实 Chroma/Embedding 写入，未实现跨存储补偿、取消确认、正式目录或正式检索。

**真实 Chroma/Embedding canary 验证结果（2026-08-27）**

- 使用唯一 UUID 作为测试用户、项目、知识库、文档和 Chunk 范围，实际加载本地 BGE，向量维度为 `512`。
- 独立 `archive_final_chunks` Collection 返回 `cosine` 配置；upsert 1 条 Chunk 后按服务端生成的
  `user_id + kb_id + document_id` 范围查询命中 1 条，随后删除 1 条并核验该范围残留为 0。
- 该 canary 只验证真实 Chroma/BGE 适配器的写入、查询、删除和清理，不创建 PostgreSQL 业务记录，
  也不等同于真实确认路由触发的 `ArchiveOperation(INDEX)`、跨存储补偿、P95 延迟标定或正式检索验收。

**取消确认基础切片完成结果（2026-08-27）**

- 新增 `POST .../cancel-confirmation`，使用 `expected_version` 和 `CONFIRMED` 状态前置校验；成功后提交
  `PENDING_RECONFIRMATION`，清除确认/索引事实并写入脱敏 `ARCHIVE_CONFIRMATION_CANCELLED` 审计。
- 数据库状态先提交，再按 `user_id + project_id + kb_id + document_id` 清理 Final Chunk；清理失败仍保守
  排除正式范围并返回稳定错误。确认路由相关测试 `9 passed`，P09 服务相关回归 `20 passed`，全量回归
  `193 passed, 2 skipped, 16 warnings`，并通过 `compileall`。
- 当前路由回归测试对外部清理使用 stub；真实确认—INDEX 纵向链路、P95 延迟标定和正式检索仍待验收。

**真实取消确认清理验证结果（2026-08-27）**

- 使用当前本地配置创建唯一 UUID 范围的临时 PostgreSQL 业务记录，并通过真实 BGE 生成 512 维向量，写入
  `archive_final_chunks` 后直接调用取消确认服务。
- 取消确认后数据库状态为 `PENDING_RECONFIRMATION`、版本从 7 递增至 8，确认事实被清空，脱敏
  `ARCHIVE_CONFIRMATION_CANCELLED` 审计保留 1 条；Chroma 精确范围删除 1 条，删除后残留为 0。
- 验证结束后已按外键顺序清理 PostgreSQL 临时记录，并再次确认 Chroma 该范围残留为 0。本验证是服务级真实清理证据，
  不等同于真实确认—`ArchiveOperation(INDEX)`—确认路由纵向链路。

### AV1-P10 清单关联、处理列表、正式目录与审计

**目标与范围**

- 实现已确认档案与同项目清单项的人工确认关联、关联建议、失效和缺失重算。
- 实现文档处理列表（`documents INNER JOIN archive_documents`，全部未删除状态）与正式档案目录（仅已确认且无可见性阻断）及结构化筛选。
- 实现需求规定的脱敏业务审计查询与受控操作类型筛选。

**验证重点**

- 档案存在但未确认关联时，必需项仍显示缺失；取消确认、删除关联或清单关键字段修改后立即重算。
- 处理列表可见待确认/失败资料；正式目录和详情永不泄露待确认或待重新确认草稿。
- 审计仅返回脱敏摘要，自动重试不重复写成功审计，其他项目审计不可查询。

**完成结果（2026-08-27）**

- 已实现处理列表、正式目录/详情、结构化筛选、清单关联建议/确认/删除和脱敏审计查询；正式目录
  只返回 `CONFIRMED` 且无可见性阻断的档案。
- P10 定向 API 测试覆盖处理列表与目录隔离、筛选、关联派生、跨项目拒绝、项目审计范围和受控操作
  类型；新增审计筛选枚举 RED→GREEN 后，P10 相关测试为 `6 passed`。

### AV1-P11 正式检索与相关性阈值标定

**目标与范围**

- 实现 `archive-retrieval`，先从 PostgreSQL 获取正式且可见的 `document_id` 集合，再用服务端注入的 `user_id + project_id + kb_id + document_id` 过滤 Chroma。
- 基于 AV1-P02 固定问题集完成门槛 2.2，冻结 Chroma 距离度量类型、分数方向和 `min_relevance_score`，写入配置与验收说明。

**验证重点**

- 8 个有据问题中至少 7 个结果包含标准证据；2 个无据问题过滤后为空；2 个隔离问题不返回其他项目数据。
- 只返回正式原文 Chunk；待确认、待重新确认、人工无证据字段和删除阻断档案不能出现。

**完成结果（2026-08-27）**

- 已实现 `archive-retrieval`：先从 PostgreSQL 获取当前项目的 `CONFIRMED` 文档集合，再以服务端
  注入的 `user_id + project_id + kb_id + document_id IN (...)` 过滤 `archive_final_chunks`，并返回
  文件名、定位、摘录和分数；外部失败与无效结果映射为稳定错误。
- 服务/API 定向测试通过；真实单文档 canary 使用 BGE 512 维、Chroma cosine，命中 1 条，10 次请求
  P95 约 `125.97 ms`。固定 8 个有据/2 个无据问题集尚未运行，`retrieval_distance_threshold` 尚未冻结。

**P14 真实固定集复验（2026-08-28，未通过）**

- 以 P02 的 12 个虚构问题，经 PostgreSQL、确认—INDEX、真实 BGE 和 Chroma 运行三次纯稠密对照；三次均保持项目隔离 `2/2`，但不存在同时满足“有据至少 `7/8`、无据为空 `2/2`”的单一 cosine distance 阈值。
- 原文不带字段上下文时，完整候选有据为 `7/8`，在拒绝全部无据候选的阈值上限降为 `2/8`；确认字段“字段名+值”为 `8/8` 与 `5/8`；仅确认字段值为 `8/8` 与 `6/8`。三次完整候选均使无据问题出现候选，故不能冻结 `min_relevance_score`。
- 本次没有降低既定 `7/8` 门槛，也没有在该次复验中引入混合检索或 Rerank。后续用户已确认只增加本地 Reranker；P11 固定集质量与依赖其阈值的 P12 真实问答质量仍待重排实现和真实复验后继续。

**P14 阶段 C 真实固定集（2026-09-01，未通过）**

- 本地 `BAAI/bge-reranker-base` 仅对完成授权与元数据校验的 Chroma Top-20 候选重排；接口保留兼容 dense `score`，以 `reranker_score` 作为最终排序和阈值依据。
- 在健康的本机 API、PostgreSQL、Chroma、512 维 BGE 与本地 Reranker 链路上运行 P02 的 12 题：12 份文档共 63 个字段上下文 Chunk、零覆盖文档为 0。完整候选为有据 `7/8`、无据拒答 `0/2`、隔离 `2/2`；拒绝全部无据候选时为有据 `3/8`、无据拒答 `2/2`、隔离 `2/2`。
- 不存在满足既定门槛的重排阈值，故不冻结任何检索阈值，也不开始 P12 真实 DeepSeek 质量验收。验收临时数据已按账号、项目、文档、Final 向量和内部知识库范围复核清理。

**P14 阶段 D1 bge-base-zh-v1.5 隔离实验（2026-09-02，未通过）**

- 在不修改正式 `.env`、默认 bge-small/512 维配置和正式 Collection 的前提下，以进程级环境变量加载 bge-base/768 维，并写入独立实验 Collection；Reranker、Top-30、C4-A 查询表达、`evidence_values`、Final Chunk 和 AV1-P02 固定集保持不变。
- 前置验证确认 768 维健康可用，512→768 维混写被 Chroma 拒绝；新增重建脚本的显式 Collection 注入不创建 `ArchiveOperation`。定向测试 `11 passed`，全量后端回归 `279 passed, 2 skipped, 48 warnings`，`compileall` 通过。
- 真实固定集结果为候选池 `12/12`、公开覆盖 `8/8`、严格候选池命中 `6/8`、无据拒答 `0/2`、隔离 `2/2`、P95 `3859.89 ms`。公开召回虽提升，但无据门槛失败，默认模型不切换，C4-C 阈值标定和 P12 真实 DeepSeek 质量仍阻塞。
- 实验 Collection 已删除，正式 Collection 保持 `0` 条；PostgreSQL 临时用户为 `0`、项目恢复为实验前 `4`、文档和 CONFIRMED 文档为 `0`，P14 临时文件无残留。

**P14 阶段 D2 Reranker 延迟前置验证（2026-09-03，large CPU 延迟未通过）**

- 使用 `scripts/p14_d2_reranker_latency_probe.py` 对固定 12 道问题、每题 30 个长度受控候选执行 3 次预热和 12 次正式推理；未访问 PostgreSQL、Chroma，未启动 API。
- 本机 20 逻辑核、15.8 GiB 内存、CUDA 不可用。`bge-reranker-large` 默认变体 P95 为 `12885.233 ms`，`max_length=256 + batch_size=8` 最佳 P95 为 `12016.218 ms`，峰值工作集约 `2.30 GiB`；均超过 `8000 ms` 风险线。
- 本次只证明当前 CPU 延迟风险，不代表检索质量；完整路径 A 固定集实验暂缓，不切换默认 Reranker，不重建 D1 隔离 Collection。详细数据见 `docs/review/P14-rag检索质量改进/P14-D2-Reranker延迟前置验证方案.md`。

### AV1-P12 带证据问答

**目标与范围**

- 实现 `archive-questions`：复用 AV1-P11 的正式检索范围和内部前 8 个候选；不以冻结阈值过滤为空作为问答拒答条件，候选为空时短路且不调用 DeepSeek，候选非空时由一次结构化 DeepSeek 调用同时完成证据充分性判定和回答。
- 模型输出固定为 `decision: ANSWERED | REFUSED_NO_EVIDENCE`、`answer` 和 `citation_numbers`（1-based 整数）；服务端校验编号并映射为既有文件名、定位和原文摘录响应，外部 API 形状不变。

**验证重点**

- 模型关闭/不可用返回 `ARCHIVE_ANSWER_UNAVAILABLE`，不影响目录、清单和人工归档。
- 以 12 个固定问题完成门槛 3.2：有据题回答并正确引用至少 `7/8`、无据拒答 `2/2`、项目隔离 `2/2`，无伪造引用、无跨项目或未确认内容。

**完成结果（2026-08-27）**

- 已实现正式证据问答：无检索依据时返回 `REFUSED_NO_EVIDENCE` 且不调用模型；有依据时只把检索
  摘录放入提示词；模型不可用映射为 `ARCHIVE_ANSWER_UNAVAILABLE`。
- 服务/API 定向测试通过；未运行真实 DeepSeek，也未完成 12 个固定问题的引用正确率验收。

**P14-D5 结构化问答更新（2026-09-08，正式质量未通过）**

- 已将问答决策收口为一次结构化 JSON DeepSeek 调用：同一输出同时给出 `decision`、`answer` 和
  `citation_numbers`；空候选继续短路返回固定拒答，非空候选的引用由服务端按 1-based 编号映射，
  外部响应契约保持不变。
- 正式 AV1-P02 12 题已在 D1 base/768、`evidence_values`、base Reranker、Top-30、`c4_a`、独立
  Collection 和真实 DeepSeek 下执行：候选池完整 `12/12`，Top-5 公开覆盖 `6/8`，有据回答并正确
  引用 `5/8`、无据拒答 `2/2`、项目隔离 `2/2`；质量门未通过，P12 继续阻塞。
- 三个失败分别为：Q-01 按 2026-09-08 用户决议，合同资料的 `DOCUMENT_DATE` 表示合同签订日期，
  因此按既定 Ground Truth 定性为漏答/证据充分性判定失败；Q-02 目标设计说明编制单位未进入 Top-5；
  Q-07 使用其他文档的 `V1.0` 回答目标设计技术说明并产生错误引用。下一步按 D6 方案处理后两类问题，
  未经授权不修改模型、候选数量、Chunk、查询表达、固定集或门槛。
- Pixie 24 个待评分项、dataset analysis、action plan 和 Step 6 verifier 均已完成；每条正式结果仍有
  两个重叠 `llm_span_trace`，其输入输出/token 相同但时间略有差异，重复原因尚未核实。静态代码和
  单测只证明一次应用层 `model.invoke`，不能据此宣称真实网络请求次数。
- 后端完整回归为 `375 passed, 2 skipped, 90 warnings`，`compileall` 通过；PostgreSQL 临时范围、
  实验 Collection 和临时文件均已核对归零，临时 API 已停止。

**P14-D6 文档绑定与 Top-8 更新（2026-09-08，实验基线质量门通过）**

- D6-A 已为候选增加请求内可信文档绑定；D6-B 经独立授权只把问答候选从 Top-5 扩至 Top-8，
  未改变 Prompt 指令、模型、Embedding、Reranker、Top-30、Chunk、查询表达、固定集或门槛。
- D6-B 在 D1 base/768、`evidence_values`、base Reranker、Top-30、`c4_a` 和独立 Collection 下
  完成真实 PostgreSQL、Chroma 与 DeepSeek 固定 12 题：候选池完整 `12/12`，Top-8 公开覆盖
  `8/8`，最终有据回答并正确引用 `7/8`、无据拒答 `2/2`、隔离拒答 `2/2`，达到门槛 3.2。
- Q-02 与 Q-07 已正确回答和引用；Q-01 的第一候选含准确合同日期但仍错误拒答，留作后续独立
  证据充分性质量事项。该结果证明实验基线的 P12 真实质量门通过，不代表正式默认 bge-small/512
  或正式 Collection 已切换。
- D6-B TDD RED/GREEN、完整回归 `380 passed, 2 skipped, 105 warnings`、`compileall`、Pixie Step 6
  和清理均通过；实验 Collection 已删除，临时 API 已停止。

**P14 正式 Embedding/Collection 切换（2026-09-08，已完成）**

- 用户确认采用 `bge-base-zh-v1.5`、768 维与 `evidence_values`，正式目标保持
  `archive_final_chunks`。仓库默认、`.env.example` 与 Compose 已同步。
- 安全重建脚本只允许精确正式目标，正式 Collection 非空时拒绝删除，并在失败时恢复为空 cosine
  Collection；768 维 canary 会在验证后删除，重建不创建业务操作记录。
- 本次重建前 PostgreSQL 文档、确认档案和正式 Collection 均为 0；实际结果为 0 文档、0 Chunk。
  临时 API 联合健康检查确认 API、PostgreSQL、Chroma 与 Embedding 全部正常，实际向量维度为 768。
- 主审相关测试 `34 passed`，全量回归 `391 passed, 2 skipped, 105 warnings`，`compileall` 通过。
  项目 `.env` 未读取或修改，其中可能存在的私有覆盖值不在本次验证范围内。

### AV1-P13 物理删除与跨存储恢复

**目标与范围**

- 实现任意稳定状态发起的物理删除：先创建 `DELETE + RUNNING + visibility_blocking`，再清理 Final Chunk、原文件、快照、字段、证据和清单关联。
- 对确认/删除跨 PostgreSQL、文件系统、Chroma 的部分失败记录 `last_completed_step`，复用确认或 `DELETE` 入口完成可重入恢复。

**验证重点**

- 删除成功后只保留脱敏删除审计；项目容量只在业务记录实际移除后递减。
- 用故障注入覆盖 Chroma 失败、文件删除成功但 PostgreSQL 失败等情形；恢复不得重复写数据、泄露可见性、或重复记录成功审计。
- 达成门槛 3.1 后才能将 FR-036/FR-040 的集成验收标为通过。

**完成结果（2026-08-27）**

- 已实现物理删除服务：先写入 `DELETE + RUNNING + visibility_blocking`，再按 Chroma、文件、数据库
  顺序清理；失败时写入稳定错误和 `DELETE_FAILED`，保持正式范围外，重试复用未完成操作。
- P13 服务/API 定向测试通过，覆盖成功清理、Chroma 失败、文件删除后数据库失败、重试互斥和脱敏
  删除审计；尚未完成真实 PostgreSQL/Chroma/文件系统故障注入 canary，因此门槛 3.1 仍未宣称通过。
- 文件删除会在标准 `kb_id/document_id/filename` 布局中依次尝试清理空文档目录和空知识库目录；
  同一知识库仍有其他文档、目录被占用或路径不属于标准布局时保留目录，避免递归误删。
- 该目录清理规则按 TDD 覆盖空目录删除与同知识库其他文档保留；文件删除、P13 服务和相关路由测试
  `33 passed, 8 warnings`，随后全量测试 `273 passed, 2 skipped, 46 warnings`，`compileall` 通过。

**P14 恢复验收准备（2026-08-28）**

- 已新增真实故障恢复运行器：健康服务建立虚构正式档案，故障服务以不可达 Chroma 触发稳定 `DOCUMENT_DELETE_INCOMPLETE`，随后由健康服务重试删除；运行器要求分别核对故障后的可见性阻断与原文件保留，以及恢复后的 PostgreSQL、Chroma、原文件归零和单条脱敏删除审计。
- 运行器的纯判定和既有删除服务定向测试已通过；受限本机未能启动临时故障服务，真实故障链路尚未执行，不能标记为通过。

**真实跨存储恢复验收结果（2026-09-02）**

- 使用健康本机 `8000`、真实 PostgreSQL、Chroma 与文件系统，并启动仅回环监听的 `8002` 故障服务，使其 Chroma 连接不可用。
- 故障删除返回 `DOCUMENT_DELETE_INCOMPLETE`，文档随后处于可见性阻断状态且原文件仍保留；健康服务重试删除成功。
- 运行器的 `failure_reported`、`visibility_blocked`、`file_preserved_before_retry`、`recovery_delete_succeeded`、`postgres_zero`、`chroma_zero`、`file_zero`、`audit_retained`、`cleanup_completed` 全部为 `true`；临时范围已复核为零，故障服务已停止。
- 本结果完成 P13 门槛 3.1 的真实恢复证据，不代表 P12 真实 DeepSeek 质量或 Vue 完整端到端验收已经通过。

### AV1-P14 全链路验收与交付文档

**目标与范围**

- 以虚构资料完成从项目创建、上传、解析、AI/手工草稿、确认、关联、检索、问答、删除的相邻 Vue 工作台演示；Swagger/OpenAPI 保留为逐接口诊断与故障定位入口。
- 在 `mini-rag-milvus-vue` 以 TDD 依次完成 FR-031 清单、FR-032/033 上传解析重试、FR-034/035 草稿建议、FR-036 确认/取消确认、FR-037 关联、FR-038 目录、FR-039 检索问答、FR-040 删除和 FR-041 审计的 DTO、API、状态、视图与测试；不以 `UnavailablePanel` 或模拟数据作为完成证据。
- 汇总单元、数据库集成、Chroma 集成、API、Vue 代理联调和端到端测试；将实测结果、环境限制和未验证项写入验收报告、README 与演示文档。

**完成条件**

- 每一项 AC-FR-030～AC-FR-041 都有可追溯测试或明确的不适用/未验证说明。
- 不把历史 Agent 结果、mock 结果或单问题在线结果写成智慧档案 V1 的质量结论。
- Vue 端每个可观察行为先有实际运行的 RED 测试，再以最小实现转为 GREEN；相关 `npm run typecheck`、`npm run test` 与 `npm run build` 通过后，使用 Vite `/api` 与真实后端进行一次无模拟数据的联调。
- Vue 工作台必须携带 Bearer Access Token，绝不发送 `X-User-ID`，不保存密码或服务端授权字段；其测试与页面不得替代固定问题集、真实 DeepSeek 或 P13 真实跨存储恢复证据。
- 文档明确 Vue 工作台是 P14 本地界面、Swagger 是契约诊断入口、样本/清单为虚构演示规则，且 DeepSeek 可能接收已脱敏片段。

## 5. 测试层次与责任边界

| 层次 | 主要任务 | 不应替代的证据 |
|---|---|---|
| 单元测试 | 哈希、状态机、字段校验、Parser 归一化、提示/响应解析、服务规则 | PostgreSQL/Chroma 的真实约束与检索质量 |
| PostgreSQL 集成测试 | 迁移、唯一约束、版本冲突、所有权、派生缺失、审计 | 文件/Chroma 跨存储恢复 |
| Chroma 集成测试 | Collection、向量维度、过滤、删除、重启持久化、检索阈值标定 | DeepSeek 回答质量 |
| API 测试 | 路由、HTTP 状态、Schema、越权、分页、稳定错误码 | 外部模型实际质量 |
| 端到端/验收 | 固定虚构资料上的业务闭环、门槛 2/3 | 生产级容量、可用性或安全承诺 |

每个任务开始时必须先列出该任务关联的 `FR`、验收标准、具体文件、测试命令和预期结果；
任务结束后只报告实际运行过的命令与结果。未运行的 PostgreSQL、Chroma、BGE 或 DeepSeek 依赖
必须单独标注，不得以静态检查替代。

## 6. 当前状态与下一步

> **最终状态（2026-09-11）：** AV1-P01～P14 的本地 V1 功能与验收收口已完成。后端完整回归
> `417 passed, 2 skipped, 121 warnings`，前端完整回归 `118 passed`，类型检查、构建和
> `compileall` 通过。D6-B 固定集达到 `7/8、2/2、2/2`，企业扩展集达到有据 `12/12`、
> 无据 `4/4`、隔离 `4/4`；P13、FR-040、FR-041 真实链路均已清理临时范围。
> 下一步为前端 URL 与 Store 项目 ID 一致性修复，以及工作区范围审查和显式 Git 提交。
> 以下带日期段落保留各阶段当时事实，不覆盖本段。

> **P14 历史收口状态（2026-08-28）：** P09～P13 实现切片已完成。P10～P13 定向测试在新增审计
> 枚举契约后为 `20 passed`；P11 真实单文档 canary 的 BGE/Chroma 512 维 cosine 检索命中 1 条，
> 10 次请求 P95 约 `125.97 ms`。12 个固定虚构问题的三种纯稠密对照未找到同时满足“有据至少 `7/8`、
> 无据拒答 `2/2`”的阈值，故 P11/P12 真实质量尚未通过。后端当前全量回归为 `237 passed, 2 skipped, 36 warnings`，并通过
> `compileall`。相邻 Vue 工作台已完成 FR-031 清单 CRUD 的 DTO/API、Pinia Store、页面和正式
> `5173/api → 8000` 真实代理 canary；API、Store 与组件的有效 RED 已记录，FR-031 完成时的检查点为 4 个测试文件、
> 23 个单元测试，typecheck 和 build 通过。canary 覆盖健康、注册/登录、项目、清单 CRUD 与注销，临时用户/会话/项目/知识库清理为零。
> FR-032/033 项目级上传、处理列表、首次解析与专用解析重试的 DTO/API、Store、页面和真实文件代理也已按 TDD 完成：
> API 方法、处理列表方法、四个 Store 行为和组件文件缺失的 RED 转为前端全量 5 个测试文件、35 个测试，typecheck/build 通过。
> 两份虚构 TXT 的 canary 先为 `201/UPLOADED`，有效文本变为 `200/PARSED`，无有效文本的普通解析和专用重试均为
> `422/PARSE_TEXT_UNAVAILABLE`，最终状态为 `PARSED` 与 `PARSE_FAILED`。首次清理因 Chroma 暂不可用保守中断；心跳恢复后由
> 既有物理删除服务完成测试范围清理并核对为零。FR-034～FR-041 尚未接入。P13 真实故障恢复
> 运行器和判定测试已准备完成，但临时故障服务尚未实际启动。P14 尚需确认 P11 质量改进边界、完成
> P12 DeepSeek、P13 真实故障恢复、逐项前端 TDD/真实代理联调与最终文档一致性检查；这些完成后才结束 P14。

> **P14 阶段 B 代码记录（2026-08-31）：** 已新增 `evidence_values` 模式，使已确认且具当前
> `FieldEvidence` 的字段值只进入定位/锚点匹配的 Final Chunk 向量输入；原文、引用定位、Chunk ID 和
> 旧三种模式均不变。映射、无证据/未确认字段排除、快照/锚点核对、稳定字段顺序和索引入口的定向测试为
> `21 passed`。Codex 新进程无法新建远程 PostgreSQL 连接，故本轮未能用临时服务完成真实固定集重建；
> 该模式尚未通过固定门槛，默认模式与阈值仍不冻结，阶段 C 不得开始。

- AV1-P01、AV1-P02、AV1-P03 已完成。P03 已新增归档 SQLModel、`ProjectContext` 所有权依赖与 `0005_archive_v1_schema`～`0008_legacy_business_comments` 前向迁移；目标 PostgreSQL 空库已实际迁移至当前版本，且全部 16 张业务表、143 个字段的注释均可由 PostgreSQL 元数据读取。
- P03 离线迁移 SQL、SQLite 约束测试和编译已通过。P09 第四个基础切片后的默认全量回归为 `191 passed, 2 skipped, 16 warnings`；其中 PostgreSQL 并发测试需要显式设置 `RUN_POSTGRES_CONCURRENCY_TEST=1`，专用 `POSTGRES_TEST_URL` 自动化迁移测试仍未配置。
- P04 前置结构调整已完成：归档 SQLModel 已按 `project.py`、`archive.py`、`checklist.py`、`archive_audit.py` 拆分，`app.models` 公共导入入口与表名、字段、约束保持不变；模型元数据、迁移/授权/注释测试和编译均已通过。本调整不新增迁移，也不实现项目或清单 API。
- P04.1 FR-030、P04.2 FR-031 与 P05 FR-032 已完成。P05 的上传、重复、容量、类型、越权与跨项目隔离已通过 SQLite/TestClient 契约测试；当前 PostgreSQL 开发库的 99→100 并发验证为一成功、一容量拒绝，UUID 范围测试数据与临时文件均已清理。AV1-C01 的历史独立实验和 AV1-C02 的本地代码/单测/命名空间/Docker
  健康验证均已完成；当前运行和开发基线为本地。云端部署与完整栈资源验证暂定至 V1 功能完成后。
  P06 已完成首次解析、快照创建、受控失败记录、专用解析重试、四格式路由和快照保护；P07 已完成手工草稿、字段证据、人工检查和重新确认入口；P08 已完成 AI 建议、失败重试、证据校验和安全 regenerate；P09 已完成确认/INDEX/取消确认切片及真实清理验证；P10～P13 已完成清单关联/目录/审计、正式检索、证据问答和物理删除切片。P11 单文档 canary 的 10 次请求 P95 约 `125.97 ms`；该句为 2026-08-28 历史状态，当前 P14 结果以上方最终状态为准。
- AV1-P02 已解除对 AV1-P11 阈值标定和 AV1-P12 问答质量验收的“验收资料缺失”阻塞；
  P11/P12 实现切片已完成，固定问题集质量、真实 DeepSeek 质量和 P13 真实故障恢复仍属 P14 的未验收项。
