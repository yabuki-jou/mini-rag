# 智慧档案 V1 验收报告

## 1. 文档信息

| 项目 | 当前事实 |
|---|---|
| 验收范围 | FR-030～FR-041、AV1-P01～P14、本地相邻 Vue 工作台；2026-09-17 追加 FR-042 后端 |
| 验收日期 | 2026-09-11；验收后修复更新至 2026-09-12；FR-042 追加至 2026-09-17 |
| 代码基线 | FR-030～FR-041 为历史 `main` 基线；FR-042 位于未提交分支 `codex/fr-042-archive-agent-mvp` |
| 当前结论 | 既有 V1 通过；FR-042 固定质量门、真实后端闭环和 Vue 确定性接入通过；完整 Vue 业务代理联调待恢复 Chroma |
| 权威状态入口 | `docs/stage/handoff.md`；实施计划见 `docs/implementation/`，P14 诊断与评审材料见 `docs/review/` |

本报告以当前代码、Git 状态和 2026-09-11 实际复验为准。此前 D5、纯稠密阈值、
FR-034～FR-041 尚未接入等结论是历史阶段事实，不再代表当前状态。

## 2. 总体验收结论

智慧档案 V1 已完成项目、清单、上传、四格式解析、AI/人工草稿、字段检查、确认与正式索引、
取消确认、清单关联、处理列表、正式目录、项目内检索、带证据问答、物理删除和脱敏审计查询。
相邻 Vue 工作台已接入 FR-030～FR-041 的当前公开契约。

P14-D6-B 在冻结的 12 题固定集上达到有据回答并正确引用 `7/8`、无据拒答 `2/2`、
项目隔离 `2/2` 的既定质量门。正式默认已切换为 bge-base/768、`evidence_values` 和
`archive_final_chunks`，正式 Collection 在空业务库条件下完成安全重建和 768 维 canary。
Q-01 仍是有直接证据时的安全漏答，因此“质量门通过”不表示所有有据问题都能回答。

2026-09-10 的企业规模扩展资料包含 16 份文档、4 份共 30 页 PDF、约 26 万字符和 20 题。
修正一项测试标注范围后，真实 Top-8 捕获与 DeepSeek 复测达到有据 `12/12`、无据 `4/4`、
隔离 `4/4`、响应契约 `20/20`。该扩展集用于补充稳定性证据，不替代冻结的 AV1-P02 门槛。

## 3. 证据等级

| 等级 | 能证明的内容 | 不能替代的内容 |
|---|---|---|
| 确定性测试 | 状态机、校验、错误码、授权、分页、事务与前端交互 | 真实模型语义、真实外部存储 |
| 真实本地链路 | PostgreSQL、文件、Chroma、BGE、DeepSeek 或 Vite 代理的实际行为 | 云端容量、生产可用性 |
| 浏览器 DOM | 用户在真实页面中的点击与可见结果 | 后端持久层完整性 |

本轮 Codex browser provider 返回 `nodeRepl.fetch request failed`。FR-040/041 的页面行为由 Vue
组件/View 自动化证明，真实数据路径由 `5173/api → 8000` 验证；两者不冒充新的浏览器点击证据。

## 4. AC-FR-030～AC-FR-041 追踪

共 45 项验收标准。下表逐项给出当前证据；“通过”仅表示对应证据等级内通过。

| 需求 | AC | 主要证据 | 结果 |
|---|---|---|---|
| FR-030 项目管理 | 01 | `tests/routers/test_projects.py::test_create_project_creates_private_knowledge_base_and_demo_checklist` | 通过：项目、私有知识库和五项虚构模板原子创建 |
|  | 02 | `test_project_name_is_trimmed_and_unique_within_one_user` | 通过：规范化同名返回冲突且不重复创建 |
|  | 03 | `test_delete_project_with_any_document_is_rejected_without_mutation` | 通过：非空项目拒绝删除且状态不变 |
|  | 04 | `test_other_user_cannot_access_project` | 通过：跨用户读写删除被拒绝 |
| FR-031 清单 | 01 | `tests/routers/test_projects_checklist.py::test_list_demo_checklist_derives_missing_and_not_provided` | 通过：模板独立且初始状态由服务端派生 |
|  | 02 | 同上及 `test_list_checklist_counts_only_confirmed_document_and_link` | 通过：必需项无确认关联为 `MISSING` |
|  | 03 | `test_update_description_preserves_link_but_matching_change_invalidates_it` | 通过：匹配字段变化使旧关联失效并重算 |
| FR-032 上传 | 01 | `tests/routers/test_project_documents.py::test_upload_txt_creates_unparsed_project_document_and_archive_record` | 通过：原文件、文档和 `UPLOADED` 状态建立 |
|  | 02 | `test_duplicate_project_file_returns_existing_summary_without_new_resources` | 通过：同项目同哈希返回既有资源 |
|  | 03 | `test_same_file_hash_can_be_uploaded_to_another_project_without_leakage` | 通过：不同项目可独立上传且不泄露 |
|  | 04 | `test_upload_larger_than_twenty_mebibytes_creates_no_project_resources`、`test_upload_when_project_has_one_hundred_documents_creates_no_resources` | 通过；99→100 的真实 PostgreSQL 并发曾验证一成功、一容量拒绝 |
| FR-033 解析 | 01 | `test_parse_project_document_supports_all_frozen_formats`、Parser PDF 测试 | 通过：文本型 PDF 为 `PARSED` 且保留页码定位 |
|  | 02 | `test_parse_short_txt_persists_parse_failed_summary` 及 PDF 有效文本规则测试 | 通过：无有效文本进入稳定失败态且保留原文件 |
|  | 03 | `test_parse_again_after_parsed_does_not_replace_snapshot` | 通过：重复普通解析被拒绝且快照不变 |
| FR-034 AI 建议 | 01 | `tests/routers/test_project_document_suggestions.py::test_first_suggestion_creates_ai_draft_with_current_snapshot_evidence` | 通过：固定七字段草稿和当前快照证据 |
|  | 02 | 建议解析/空字段测试与真实模型格式修复链路 | 通过：缺失字段保持空且不补造证据 |
|  | 03 | `test_regenerate_replaces_unedited_ai_draft_and_records_audit` | 通过：未人工编辑草稿可安全原子替换 |
|  | 04 | `test_regenerate_rejects_manual_edit_without_calling_model`、`test_regenerate_failure_preserves_existing_ai_draft` | 通过：人工内容不被覆盖，失败保留原稿 |
| FR-035 人工草稿 | 01 | `tests/routers/test_project_document_draft.py::test_manual_draft_creates_seven_pending_fields` | 通过：`PARSED` 创建七个待检查空字段 |
|  | 02 | `test_manual_draft_can_fallback_from_suggestion_failed`、`test_suggestion_failed_fallback_replaces_partial_ai_draft` | 通过：建议失败可降级为空白人工草稿 |
|  | 03 | 人工草稿、字段保存与 FR-034/035 真实代理链路 | 通过：AI 不可用路径不依赖模型即可继续 |
|  | 04 | `tests/routers/test_project_document_confirmation.py::test_confirm_rejects_unchecked_fields_without_mutating_document` | 通过：存在待检查字段时拒绝确认 |
|  | 05 | `test_manual_draft_can_read_and_save_manual_field_without_evidence` | 通过：人工来源与无证据标记持久化 |
| FR-036 确认 | 01 | `test_confirm_transitions_document_and_writes_redacted_audit`、真实确认—INDEX canary | 通过：确认、Final Chunk 和正式可见性成立 |
|  | 02 | `test_editing_confirmed_field_enters_pending_reconfirmation`、FR-036 真实链路 | 通过：编辑后退出正式范围，重确认后恢复 |
|  | 03 | `test_confirm_is_idempotent_for_current_confirmed_version` | 通过：同版本重复确认幂等且不重复 INDEX |
| FR-037 关联 | 01 | `tests/routers/test_project_archive_catalog.py::test_checklist_link_suggestions_confirm_delete_and_audit`、FR-037 真实链路 | 通过：只有建议而未确认关联时仍缺失 |
|  | 02 | 同上 | 通过：确认同项目关联后为 `SATISFIED` |
|  | 03 | 同上及取消确认真实链路 | 通过：删关联或取消确认后立即恢复缺失 |
|  | 04 | `test_checklist_link_rejects_nonconfirmed_document_and_cross_project_item` | 通过：跨项目关联被拒绝且不泄露资源 |
| FR-038 双列表 | 01 | `test_processing_list_and_formal_archive_detail_are_separate`、FR-038 真实代理链路 | 通过：处理列表含全部未删除状态，正式目录仅确认档案 |
|  | 02 | `test_formal_archive_excludes_visibility_blocked_document_and_supports_filters` | 通过：阶段/日期筛选与稳定倒序 |
|  | 03 | 同上及 `ArchiveCatalogPanel` 前端测试 | 通过：日期为空与日期区间互斥并精确筛选 |
|  | 04 | 合同日期标签前端测试及 FR-039 真实合同式资料链路 | 通过：合同按签订日期解释，其他资料按文档日期解释 |
| FR-039 检索问答 | 01 | `tests/services/archive/test_questions.py::test_answer_uses_only_retrieved_evidence_and_returns_citations`、D6-B/企业扩展集 | 通过：仅依据当前项目正式原文并返回引用 |
|  | 02 | `test_no_evidence_refuses_without_calling_model`、固定集无据 `2/2`、扩展集 `4/4` | 通过：无据固定拒答且无伪造引用 |
|  | 03 | 正式范围过滤测试和 FR-039 真实页面链路 | 通过：待确认及待重新确认档案被排除 |
|  | 04 | `tests/services/archive/test_retrieval.py::test_retrieval_uses_formal_scope_and_filters_result_documents`、固定集与页面隔离链路 | 通过：其他项目内容不进入候选或回答 |
|  | 05 | 明确包含“资料类型：CONTRACT”和“合同签订日期”的真实代理复测 | 通过：回答 `2026-05-20` 并只引用目标合同日期原文 |
| FR-040 删除 | 01 | `tests/services/archive/test_deletion.py::test_delete_removes_archive_file_vector_and_link_but_keeps_redacted_audit`、FR-040 真实全链路 | 通过：文件、档案、关联和向量归零，清单恢复缺失 |
|  | 02 | `test_delete_failure_keeps_document_hidden_and_retry_resumes_cleanup`、P13 故障服务 canary | 通过：失败返回稳定错误并保持可见性阻断，可重入恢复 |
|  | 03 | `tests/routers/test_project_archive_document_delete.py::test_delete_document_route_rejects_other_user_without_mutation` | 通过：跨用户 DELETE 返回 `403/PROJECT_FORBIDDEN`，删除服务未调用，数据库记录、原文件和快照不变 |
| FR-041 审计 | 01 | `tests/routers/test_project_archive_catalog.py::test_audit_log_list_is_project_scoped_and_redacted`、FR-041 真实代理链路 | 通过：返回操作人、时间、资源和脱敏摘要 |
|  | 02 | `test_connection_timeout_retries_once_before_success`、建议重试单条审计断言及 `_generate_suggestions` 单次持久化路径 | 通过（组合确定性证据）：连接超时只重试一次，最终成功只提交一条业务审计；未做真实网络断线注入 |
|  | 03 | `test_audit_log_list_rejects_other_users_without_leaking_records`、FR-041 真实双账号代理链路 | 通过：另一用户收到 `403` 且响应不含摘要 |

## 5. 当前自动化与真实链路结果

| 检查 | 当前结果 |
|---|---|
| 后端完整测试 | `487 passed, 2 skipped, 130 warnings` |
| Python 编译 | `python -m compileall -q app tests` 通过 |
| 差异检查 | `git diff --check` 通过；仅有工作区 LF/CRLF 提示 |
| 前端完整测试 | 11 个测试文件，历史最终证据 `122 passed` |
| 前端类型检查 | `npm run typecheck` 通过 |
| 前端生产构建 | `npm run build` 通过 |
| 联合健康 | `5173/api/health` 返回 200；API、PostgreSQL、Chroma、Embedding 均为 `ok`，Embedding 768 维 |
| 固定质量门 | D6-B：有据 `7/8`、无据 `2/2`、隔离 `2/2`，通过 |
| 企业扩展集 | 16 份、含 4 份 30 页 PDF、约 26 万字符；有据 `12/12`、无据 `4/4`、隔离 `4/4`、契约 `20/20` |
| P13 恢复 | 真实 PostgreSQL、Chroma、文件故障删除—恢复通过，临时范围归零 |
| FR-040 真实删除 | 删除前正式检索/问答有据；删除后数据库、Final Chunk、文件归零并拒答 |
| FR-041 真实审计 | 分页、筛选、操作人、时间、脱敏和跨用户 403 通过；临时范围归零 |

两项跳过来自需要专用 PostgreSQL 测试配置的自动化场景；当前开发 PostgreSQL 已实际完成
`0010_account_auth` 迁移与结构核对。121 条警告是既有 Starlette/FastAPI/Pixie 弃用警告，
本轮没有把警告当作失败，也没有宣称已消除。

## 6. 实际代码与旧文档差异

最终收口前发现并处理了以下差异：

1. 旧验收报告仍停留在 D5 `5/8` 和 FR-034～FR-041 未接入阶段；当前以 D6-B、企业扩展集、
   FR-034～FR-041 实际代码和真实链路结果替代该状态。
2. FR-041 设计要求响应包含 `actor_id`，旧 `AuditLogRead` 会过滤该字段；现已补齐。
3. `ARCHIVE_FIELD_UPDATED`、`PARSE_RETRIED`、`SUGGESTION_RETRIED` 原先只有筛选枚举，
   没有成功操作写入点；现已在对应事务中补齐，并保证失败路径不伪造成功审计。
4. FR-040 的旧删除测试假设同一文档只有一条审计；字段更新留痕后该假设失效，测试已改为精确筛选
   `DOCUMENT_DELETED`。
5. AC-FR-040-03 原先只有项目权限依赖和其他路由的组合证据，缺少项目文档 DELETE 的直接越权
   回归；现已增加 HTTP 特征测试，并验证目标持久化事实与文件均不变。

## 7. 未验证、限制与风险

- 本轮没有新增 FR-040/041 的真实浏览器 DOM 点击证据，原因是 Codex browser provider 无法连接。
- 没有配置 `POSTGRES_TEST_URL` 执行可重复的独立空库迁移自动化；开发库迁移实测不能替代该测试。
- 未进行云端部署、2 vCPU/2 GB 完整栈容量、长时间稳定性、生产备份恢复或多实例验证。
- PostgreSQL、Chroma 与文件系统没有分布式事务；P13 证明一次受控故障可恢复，不代表穷尽生产故障组合。
- Q-01 仍存在安全漏答；若继续优化，必须另立单变量方案，不能修改固定题或降低门槛。
- 旧项目 URL 与新会话 Store 不一致的问题已于 2026-09-12 按 TDD 修复；登录、注册、有效深链接和
  退出登录共四项回归已通过。由于 browser provider 仍不可用，本项没有真实 DOM 复验。
- 相邻 `mini-rag-milvus-vue` 不是 Git 仓库，前端改动目前只能用文件内容、测试、类型检查和构建证明，
  不能提供前端 Git 历史。

## 8. 范围结论与下一步

智慧档案 V1 的本地学习与演示范围可以标记为功能验收通过，同时保留第 7 节边界。Swagger 继续用于
API 契约诊断；Vue 是本地工作台；业务客户端只访问 FastAPI，不直接访问 PostgreSQL、Chroma 或文件系统。

V1 发布文档基线已完成；下一步由用户 review 未推送本地提交，决定 push/tag 或直接进入作品集材料。
云端部署、容量、拓扑和资源规格仍保持独立决策，不因本次发布文档收口自动启动。

## 9. 验收后修复

2026-09-12 完成前端 URL 与 Store 项目 ID 一致性修复。Luna 的真实 RED 为新增 `4 failed`、
既有 `10 passed`；GREEN 后主 Agent 复核目标 `14 passed`、前端全量 `122 passed`、
`typecheck` 和 `build` 通过。实现只修改工作台 View 与对应测试；相邻前端仍不是 Git 仓库。

2026-09-13 在验收完成后追加 services 业务域分包、评测目录收口、旧 RAG Prompt 归位以及 Agent/身份
服务级测试和 Agent 应用服务职责拆分；这些变更只调整模块边界、导入和确定性测试，不改变既有验收行为。
最新后端完整回归为 `487 passed, 2 skipped, 130 warnings`，services 定向测试为 `138 passed, 1 skipped`。

## 10. FR-042 项目档案助手追加验收

2026-09-17，FR-042 第四轮真实 DeepSeek 固定集达到严格 `17/17`：有据单轮 `8/8`、无据 `2/2`、
隔离 `2/2`、目录 `2/2`、多轮 `2/2`、隐私 `17/17`、受控失败 `1/1`。32 条人工评审无 pending，
官方 Step 6 校验通过。该质量评测使用注入的虚构、去标识化外部世界。

同日完成独立真实后端闭环：本地 FastAPI、临时 Chroma、本地 BGE/Reranker、文件、SQLite Checkpoint、
真实 DeepSeek，以及远端 PostgreSQL 中的随机隔离 Schema。验收完成虚构 TXT 的上传、解析、人工确认、
正式索引、目录回答、有据回答与同文档引用、无据固定拒答、三轮历史和脱敏工具审计。隔离 Schema
迁移至 `0011_archive_agent_scope`；结束后 PostgreSQL、Chroma、文件与 Checkpoint 均为零残留，随机
Schema 和本地临时环境已删除。

该次真实后端闭环证据不包含 FR-042 Vue 页面或浏览器 DOM，当时也没有迁移或写入共享开发库
`public` Schema。FR-042 本地修正后的完整确定性回归为 `598 passed, 2 skipped`；该次真实验收没有
修改产品代码，因此未重复运行后端全量测试。

随后相邻 Vue 仓库在 `codex/fr-042-archive-agent-mvp` 完成 FR-042 独立入口、四端点 API、Pinia 会话状态、
连续消息、历史、脱敏工具日志和项目切换隔离。TDD 后前端完整回归为 `11` 个测试文件、`130 passed`，
`npm run typecheck` 与 `npm run build` 通过。直接 `8000` 与 Vite `5173/api` 的创建会话端点均返回相同
`401 AUTHENTICATION_REQUIRED`，页面深链返回 `200`。本次重新执行标准 `python run.py` 时，Alembic 将
当前开发库 `public` Schema 从 `0010_account_auth` 升至 `0011_archive_agent_scope`；未执行回滚。
健康检查显示 PostgreSQL 和 768 维 Embedding 正常、Chroma 未运行，直接与代理均返回 `503 degraded`；
浏览器控制服务同时返回 `nodeRepl.fetch request failed`。因此不能把本轮证据写成登录后三类问题或真实
DOM 点击已通过，完整 Vue 业务代理链路仍待 Chroma 恢复后复验。

随后在用户明确授权后完成 Vue 真实代理闭环。验收使用随机隔离 PostgreSQL Schema、独立临时 Chroma
Collection、文件目录和 SQLite Checkpoint，全部业务请求均经 Vite `5173/api` 发出。虚构 TXT 完成
上传、解析、七字段人工确认、`CONFIRMED` 和正式索引；项目档案助手三轮依次得到目录 `ANSWERED`、
有据 `ANSWERED` 与固定 `REFUSED_NO_EVIDENCE`。有据响应含 1 条同文件脱敏引用，历史为 6 条完整
用户/助手消息且历史引用按 MVP 契约恒空，工具审计只有目录和证据两个只读工具，共 2 条。

本轮前三次失败分别来自验收器读取非公开 `final_chunk_count`、错误要求恢复历史引用，以及复合问题一次
真实模型判定波动；修正为公开契约断言和单一直接证据烟测后，第四次完整通过。验收前确认临时正式
Collection、文件和 Checkpoint 均有实际写入；结束后隔离 Schema、两个临时 Collection、8 个临时文件、
Checkpoint 和 ASCII 临时目录全部清理。前端深链接返回 `200`，但本轮仍没有新增浏览器 DOM 点击证据。
问题与优化记录见 `docs/review/FR-042-Vue真实代理闭环验收/`。
