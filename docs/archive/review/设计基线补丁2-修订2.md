# 实施前修订补丁 2(修订版 rev2,最终应用版)

> **适用范围**:`docs/需求说明.md`、`docs/技术架构.md`、`docs/数据库设计.md`、`docs/接口设计.md`
> **版本说明**:本文件为 `设计基线补丁2-修订1.md` 的修订版,**取代 rev1 作为最终应用版本**。相对 rev1 的修订点以 ⊿ 标注(共 3 处,见下文 S1~S3)。

---

## 修订点摘要

| 版次 | # | 修订 | 原因 |
|---|---|---|---|
| rev1 | R1~R7 | (见 rev1 原文,已通过) | — |
| rev2 | ⊿ S1 | Idempotency-Key 不启用后:`POST /checklist-items` 增加 `expected_project_version` 作为创建幂等约束;修正"不重复产生记录或审计"的过强表述 | 清单项名称允许重复且无版本约束,响应丢失重发可能产生重复记录与审计 |
| rev2 | ⊿ S2 | `suggestions/regenerate` 必须携带 `expected_version`,服务在模型返回后校验版本一致且仍无人工内容才原子替换;不满足 → 409 | 模型调用期间用户可能编辑字段,缺乏并发保护会覆盖新人工内容 |
| rev2 | ⊿ S3 | 阈值标定口径对齐需求 12.3"至少 7/8";明确相似度度量类型(如 COSINE/IP)与比较方向(越大越相关)需与 `min_relevance_score` 同步冻结 | rev1 写"8/8 全部通过"高于需求验收标准;score 语义需可解释 |

---

## 决策摘要(最终版)

| # | 事项 | 决策 |
|---|---|---|
| 1 | 分页默认值 | 固定 `page=1`、`page_size=20`、最大 `100` |
| 1 | Idempotency-Key | **不启用**;创建类接口( checklist-items)以 `expected_project_version` 保证同一版本至多创建一条;重发旧版本 → 409 |
| 1 | 问答检索参数 | `top_k` 默认 5 最大 10;问答内部固定 `top_k=5` + `min_relevance_score`(验证后冻结,口径对齐需求 12.3);问题长度 1..2000;相似度度量类型与方向同步冻结 |
| 2 | 重复 confirm 幂等 | 业务幂等:`CONFIRMED` 且 `expected_version` **等于当前版本** → 200;旧版本重放 → 409 |
| 3 | 聚合版本递增 | 字段/建议(含 retry)/重新建议/manual-draft/confirm/cancel 成功均 +1;checklist-items 创建成功 → `project.version` +1;regenerate 必须在模型返回后二次校验版本;解析不递增 |
| 4 | 列表次级排序 | 全部列表追加 `id DESC`;数据库索引同步(项目/文档/清单项/目录/审计) |
| 5 | 取消确认与关联 | 取消确认不改变 `checklist_links.status` |
| 6 | 响应示例/免责声明 | 补 `ArchiveDetail`、检索结果(Top-K 结构,含 score 语义说明);新增 §3.5 免责声明 |
| 7 | 模型格式错误码 | 新增 `422 SUGGESTION_INVALID_OUTPUT` + 完整状态语义(按首次/重试/重新生成区分) |
| 8 | 快照可替换表述 | 明确 V1 内快照一旦创建不可替换 |
| 9 | 操作记录生命周期 | 每文档每非删除类型至多一条终态记录;审计 `operation_id` 无外键可独立保留 |

---

## 补丁 1:API §3.2(分页、Idempotency-Key)+ §8(问答参数)+ §12(待确认事项)

### 1.1 分页(替换 §3.2 中 [ASK USER] 分页条目)

```text
- 列表使用 `page`（从 1 开始）和 `page_size`；默认 `page=1`、`page_size=20`、最大
  `100`。所有列表统一追加 `id DESC` 作为次级排序，见本小节排序规则。
```

### 1.2 Idempotency-Key(替换 §3.2 中 [ASK USER] 条目)(⊿ S1:重写)

```text
- 第一版不引入 `Idempotency-Key` 请求头。写操作幂等性由以下机制共同保证：
  聚合 `expected_version` 乐观锁、`archive_operations` 单 RUNNING 操作锁、
  重复确认业务幂等、上传 `file_hash` 去重、删除失败复用未完成操作。
- 对于创建类接口：`POST /checklist-items` 必须携带 `expected_project_version`
  （项目当前版本），服务在事务内校验版本一致后才创建；版本不匹配返回
  `409 VERSION_CONFLICT`，保证同一版本请求至多创建一条清单项和一条审计。
  上传通过 `file_hash` 去重、档案—清单关联创建携带
  `expected_document_version` + `expected_checklist_item_version`，均已有
  创建幂等约束。
- 不保证客户端因"响应丢失"而重发 POST 时返回第一次响应（无 Idempotency-Key
  时无法做到传输重放幂等）；只保证上述机制不重复产生文件、向量、档案事实及
  成功审计。网络自动重试仅限连接失败/超时一次。
```

### 1.2b `POST /checklist-items` 请求 Schema 修订(⊿ S1:新增)

§6 中 `POST /projects/{project_id}/checklist-items` 的请求示例增加 `expected_project_version`:

```json
{
  "name": "施工方案",
  "document_type": "CONSTRUCTION",
  "is_required": true,
  "project_stage": "CONSTRUCTION",
  "description": "每个施工阶段至少一份",
  "expected_project_version": 1
}
```

并追加规则:

```text
- `expected_project_version` 必填；服务在事务内校验项目当前版本一致后才创建清单项。
  旧版本返回 `409 VERSION_CONFLICT`。创建成功后 `projects.version` 原子 +1。
```

### 1.3 问答检索参数(替换 §8 中两处 [ASK USER])(⊿ S3:阈值口径对齐需求)

`archive-retrieval` 小节:

```text
`top_k` 固定为默认 `5`、最大 `10`。检索与过滤规则与 `archive-questions` 共用
（同一服务、同一 `top_k` 与同一 `min_relevance_score`），保证核对接口与问答
行为一致。过滤后无候选时返回 `200` 与空 `items`。
```

`archive-questions` 小节:

```text
服务先用 PostgreSQL 获取正式 `document_id` 集合，再以服务器注入的
`user_id + project_id + kb_id + document_id` 过滤 Milvus，内部检索固定
`top_k=5`，候选 Chunk 按相似度降序截取前 5 条；随后应用相关性阈值
`min_relevance_score`：仅保留相似度达到阈值的 Chunk 作为回答上下文。
过滤后无候选 → 直接返回 `REFUSED_NO_EVIDENCE`，不调用 DeepSeek，不编造引用。
问题长度限制为 `1..2000` 个字符。

`min_relevance_score` 的冻结流程（不凭空指定）：
1. 在 Parser/BGE 可行性验证阶段，使用需求 12.1 的 12~18 份验收资料与 12.3 的
   12 个固定问答用例标定阈值；
2. 标定目标（与需求 12.3 验收口径一致）：8 个有据问题中至少 7 个检索结果
   包含标准证据、2 个无依据问题全部被拒、2 个项目隔离问题不泄露任何内容、
   最终回答引用正确率 100%；
3. 相似度度量类型（如 Milvus `COSINE` 或 `IP`）与比较方向（分数越大越相关）
   必须与 `min_relevance_score` 数值在验证阶段**同步冻结**，避免阈值失去稳定
   语义；冻结后写入实施计划与验收说明。
```

### 1.4 §12 待确认事项(替换为)

```text
以下为接口技术契约选择，不改变已确认业务范围，需评审后固化：

1. [已确认] 列表分页默认 `page=1`、`page_size=20`、最大 `100`。
2. [已确认] 第一版不启用 `Idempotency-Key`，由版本锁、操作锁与幂等语义兜底；
   创建 `checklist-items` 必须携带 `expected_project_version`。
3. [已确认] `top_k` 默认 `5`、最大 `10`；问答内部固定 `top_k=5` 并使用
   `min_relevance_score`（数值、度量类型与比较方向在 Parser/BGE 可行性验证后
   同步冻结）；问题长度 `1..2000` 字符。
4. [TODO – IMPLEMENTATION] 依据本文件创建 Pydantic Schema、Router、Service、
   稳定错误码、Alembic 迁移与 API 测试；在此之前不得声称路由存在。
```

---

## 补丁 2:API §7 confirm 小节(重复 confirm 幂等)

在 `POST .../confirm` 小节末尾追加:

```text
幂等语义（业务幂等，非传输重放幂等）：
- 文档已为 `CONFIRMED` 且 `expected_version` 等于当前版本时，重复 `confirm`
  幂等返回 `200` 当前档案，不重新生成 Final Chunk、不新增成功审计。
- `expected_version` 与当前版本不一致（含网络重放旧版本）时返回
  `409 VERSION_CONFLICT`。
- 该幂等仅覆盖"同一最新版本的重复确认请求"；因响应丢失而重发旧版本请求时返回
  409 而非首次响应——V1 无 Idempotency-Key，不承诺传输重放幂等。
- `confirm` 允许的状态为 `PENDING_CONFIRMATION`、`PENDING_RECONFIRMATION`
  与幂等返回的 `CONFIRMED`；其他状态返回 `409 CONFIRM_NOT_ALLOWED`。
```

---

## 补丁 3:API §7 字段更新规则区(聚合版本递增规则)(⊿ S1、⊿ S2:修订)

在"规则："列表末尾追加:

```text
- 归档聚合版本 `archive_documents.version` 的递增规则：以下写操作每次成功原子
  +1——`PUT .../fields` 保存任一字段或证据、AI 建议首次成功（`suggestions`）、
  建议失败后重试成功（`suggestions/retry`）、`manual-draft` 创建成功、
  `suggestions/regenerate` 在二次校验通过后原子替换成功、`confirm` 成功、
  `cancel-confirmation` 成功。解析与解析重试不递增版本。
- 每次递增后，响应 `ProcessDocument.version` 必须返回最新值；客户端后续写操作
  必须携带该最新 `expected_version`，否则返回 `409 VERSION_CONFLICT`。
- ⊿ `suggestions/regenerate` 必须携带 `expected_version`（文档当前版本）。
  服务在模型调用返回后、事务内再次校验：① `archive_documents.version` 仍等于
  提交版本（未被并发修改）；② 七字段仍无人工内容（全部 `PENDING_CHECK` 且无
  `source = MANUAL` 字段或证据）。两者不满足时返回 `409 VERSION_CONFLICT`，
  不替换草稿、不递增版本、不改变状态；满足时原子替换并版本 +1。
```

---

## 补丁 4:API §3.2 排序规则 + 数据库索引(列表次级排序)

### 4.1 API §3.2 追加统一排序规则

```text
- 所有列表默认排序统一追加 `id DESC` 作为次级排序（tie-breaker）：
  项目列表 `updated_at DESC, id DESC`；文档处理列表 `updated_at DESC, id DESC`；
  正式目录 `confirmed_at DESC, id DESC`；清单项 `updated_at DESC, id DESC`；
  审计 `created_at DESC, id DESC`。
```

### 4.2 数据库索引同步(逐表修正)

```text
- §6.1 `projects`：`(owner_id, updated_at DESC, id DESC)`
- §6.2 `documents`：`(project_id, created_at DESC, id DESC)`
- §6.7 `checklist_items`：`(project_id, updated_at DESC, id DESC)`
- §6.3 `archive_documents`：`(status, confirmed_at DESC, id DESC)`、
  `(status, updated_at DESC, id DESC)`
- §6.10 `archive_audit_logs`：`(project_id, created_at DESC, id DESC)` 已含
  `id DESC`，不变
```

---

## 补丁 5:API §7 cancel-confirmation 小节(取消确认与清单关联)

在 `cancel-confirmation` 小节末尾追加:

```text
取消确认不改变 `checklist_links.status`（已确认关联保持 `CONFIRMED`）。清单
满足性由"档案 `CONFIRMED` 且关联 `CONFIRMED`"共同派生，因此取消确认后清单项
立即显示"缺失/未提供"；档案重新确认后清单状态自动恢复满足，无需重建关联。
```

---

## 补丁 6:API §4 公共响应对象 + §8 检索响应 + §3 免责声明

### 6.1 新增 §4.5 ArchiveDetail(正式档案详情响应)

```json
{
  "id": "uuid",
  "filename": "施工方案.docx",
  "status": "CONFIRMED",
  "confirmed_at": "2026-08-06T09:30:00Z",
  "confirmed_by": "uuid",
  "fields": [
    {
      "field_name": "TITLE",
      "text_value": "施工方案",
      "date_value": null,
      "json_value": null,
      "review_status": "VALUE_CONFIRMED",
      "source": "AI",
      "no_source_evidence": false,
      "evidences": [
        {
          "id": "uuid",
          "excerpt": "施工方案（版本：V1.0）",
          "location_type": "DOCX_PARAGRAPH",
          "location_start": 3,
          "location_end": 3,
          "normalized_anchor": "施工方案（版本：V1.0）"
        }
      ]
    }
  ],
  "version": 4,
  "created_at": "2026-08-06T09:25:00Z",
  "updated_at": "2026-08-06T09:35:00Z"
}
```

说明:人工无证据字段在详情中可显示,但 `no_source_evidence = true` 且不进入问答依据。

### 6.2 §8 archive-retrieval 响应示例(Top-K 结构)(⊿ S3:补 score 语义)

```json
{
  "items": [
    {
      "chunk_id": "uuid",
      "document_id": "uuid",
      "filename": "施工方案.docx",
      "location_type": "DOCX_PARAGRAPH",
      "location_start": 3,
      "location_end": 3,
      "excerpt": "编制单位：示例建设公司",
      "score": 0.62
    }
  ],
  "requested_top_k": 5,
  "returned_count": 1
}
```

说明:该接口是 Top-K 检索结果,不使用分页结构。`score` 为相似度分数,其度量
类型（如 Milvus `COSINE` 或 `IP`）与比较方向（越大越相关）由实际 BGE 向量与
Milvus 配置决定,必须与 `min_relevance_score` 在验证阶段同步冻结并写入验收
说明,使 score 成为可解释的调试字段而非依赖内部实现的黑箱值。`returned_count`
反映应用 `min_relevance_score` 过滤后的实际数量,与问答 `REFUSED_NO_EVIDENCE`
行为对应。

### 6.3 新增 §3.5 免责声明与数据提示(落实 NFR-020)

```text
### 3.5 免责声明与数据提示

- Swagger 首页描述与 README 必须展示以下内容：
  1. 外部模型数据提示：调用 DeepSeek 的接口（`suggestions`、
     `suggestions/retry`、`suggestions/regenerate`、`archive-questions`）的
     OpenAPI description 必须注明"文档片段可能发送给外部 DeepSeek，仅限虚构或
     脱敏资料"；
  2. 虚构清单免责声明："内置清单为虚构演示规则，不代表法定或行业归档要求"。
- 对应需求 NFR-020，实现时作为 Swagger 配置与 README 内容强制项。
```

---

## 补丁 7:API §7 与 §10(模型输出格式错误稳定错误码)

### 7.1 §7 建议接口段落修正

将"并返回 `503 ARCHIVE_SUGGESTION_UNAVAILABLE` 或稳定的格式错误"改为:

```text
并返回 `503 ARCHIVE_SUGGESTION_UNAVAILABLE`；模型输出不合格（无法解析为固定
字典字段或证据结构）时返回 `422 SUGGESTION_INVALID_OUTPUT`。
```

### 7.2 §10 错误码表新增一行

```text
| 422 | `SUGGESTION_INVALID_OUTPUT` | 模型返回内容无法解析为固定字典字段或证据结构；不自动重试（符合 BR-023） |
```

### 7.3 §7 补充 `SUGGESTION_INVALID_OUTPUT` 的状态语义

```text
`SUGGESTION_INVALID_OUTPUT` 的状态行为按发起场景区分：
- 首次建议（从 `PARSED` 发起）或失败后重试（从 `SUGGESTION_FAILED` 发起）遇到
  模型格式错误：文档进入/保持 `SUGGESTION_FAILED`，不自动重试；用户可再次重试
  建议或转为手工草稿。
- 重新生成建议（从 `PENDING_CONFIRMATION` 发起）遇到该错误：保留现有草稿与
  `PENDING_CONFIRMATION` 状态，原草稿不被丢弃，不转为 `SUGGESTION_FAILED`。
- 与 `503 ARCHIVE_SUGGESTION_UNAVAILABLE` 的区分：503 表示模型不可用（连接
  失败/超时/未配置，自动重试一次）；422 表示模型可访问但输出不合格（不自动重试）。
```

---

## 补丁 8:数据库 §6.4(快照可替换表述修正)

将:

```text
每个归档文档只保留一个当前、可重建的解析快照。它不是档案版本历史；重新解析替换
快照前必须先满足状态和操作锁规则。
```

替换为:

```text
每个归档文档只保留一个当前、可重建的解析快照。它不是档案版本历史。V1 中普通解析
仅允许从 `UPLOADED` 发起、重新解析仅允许从 `PARSE_FAILED` 发起（该状态无成功
快照），因此快照一旦创建在 V1 内不会被替换。若未来开放已解析文档的重新解析，需
同步设计 `field_evidences.snapshot_id`（ON DELETE RESTRICT）与快照替换的关联
迁移策略，不属于本期范围。
```

---

## 补丁 9:数据库 §6.9(内部操作记录生命周期)+ §12(验收清单)

### 9.1 §6.9 末尾追加生命周期说明

```text
操作记录生命周期（控制表增长）：

- `DELETE` 类：未完成（`RUNNING`/`FAILED`）记录必须保留至删除完成（部分唯一索引
  保证至多一条）；删除成功后 `archive_documents` 物理删除，操作记录随 ON DELETE
  CASCADE 清理，最终不残留。
- 非 DELETE 类（`PARSE`/`SUGGEST`/`INDEX`）：每次操作进入终态
  （`SUCCEEDED`/`FAILED`）后，删除该文档同类型更早的终态记录，仅保留最新一条；
  文档删除时随级联清理。因此每文档每非删除类型至多保留一条历史记录，表规模有界
  （约等于文档数 × 3 + 未完成删除操作数）。
- `RUNNING` 记录不清理；服务重启后按 `(operation_status, updated_at)` 索引扫描
  恢复未完成操作。
- 审计关联说明：清理操作记录不影响审计。`archive_audit_logs.operation_id` 是
  "历史关联 ID"，数据库未对其设外键（§6.10），仅用于"一次成功结果不重复写审计"
  的唯一性约束；操作记录被清理后，审计仍独立保留，查询审计不依赖操作记录存在。
```

### 9.2 §12 数据库验收清单追加

```text
- 每份文档每非删除操作类型至多保留一条终态记录；未完成 `DELETE` 记录保持
  `visibility_blocking` 直到删除完成。
- 清理操作记录后，对应审计记录（`operation_id`）仍可独立查询，不受影响。
```

---

## 落实后的一致性自检

- [ ] 需求文档:12.3 验收口径已与补丁 1.3 阈值标定目标对齐(至少 7/8,一致)
- [ ] 架构文档:§10.2/§11.1 无需改动;§17 风险表可补"相关性阈值需验证后冻结"
- [ ] 数据库文档:补丁 4.2、8、9;`projects.version` 随 checklist-items 创建递增
- [ ] API 文档:补丁 1、1.2b、2、3、4.1、5、6、7;POST checklist-items 新增 `expected_project_version`
- [ ] 更新各文档 §1 文档信息的状态/更新日期为 2026-08-06

*本文件为 design-baseline-patch-2 的修订版(rev2),取代 rev1。2026-08-06。*
