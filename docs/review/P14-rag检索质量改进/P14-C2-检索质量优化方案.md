# P14-C2 检索质量优化方案

## 文档状态

| 项目 | 当前值 |
|---|---|
| 文档角色 | P14 唯一当前执行计划与任务台账 |
| 当前状态 | C.2 Top-20 候选池实验已完成；质量门槛未通过；C.3 优化方案已提出，尚未授权实施 |
| 最后更新 | 2026-09-01 |
| 当前阶段 | C.3 检索质量问题分解与授权决策；不执行其他检索变量 |
| 唯一下一步 | 确认 C.3-A 内部 Top-20 诊断范围，再选择并授权一个检索变量；授权前不改变运行时行为 |
| 质量门槛 | 有据 `>=7/8`、无据拒答 `2/2`、项目隔离 `2/2` |
| 当前阻塞 | 质量门槛仍未通过：至少存在召回缺失和无据高分两个独立问题；公开接口只返回最终 Top-10，无法直接观测内部 Top-20 |
| 事实记录 | [检索质量问题分析与改进策略](检索质量问题分析与改进策略.md) |
| 决策边界 | [P14-C1 双排序诊断决策](P14-C1-双排序诊断决策.md) |

本方案只管理 P14 检索质量工作，不替代需求、技术架构、数据库、接口设计或 P12 问答验收。
真实模型质量必须以同一固定 12 题和现实 PostgreSQL、Chroma、BGE 链路为证据；Mock 测试不能
证明语义质量。

## 1. 不变量与范围

- PostgreSQL 是确认状态、项目授权和删除可见性的事实来源。
- Chroma 是唯一候选来源，查询必须包含服务端构造的 `user_id + project_id + kb_id + document_id`。
- 阶段 C 的唯一例外是本地 `BAAI/bge-reranker-base` 对已授权、已校验的 Chroma Top-20 重排。
- 不引入 BM25、混合检索、远程 Rerank、跨项目检索或无授权的 Chunk 修改。
- 引用继续使用原始 Final Chunk 的正文、文件名、定位和 Chunk ID。
- 未冻结任何 `retrieval_distance_threshold`、`archive_reranker_score_threshold` 或向量表示模式。

## 2. 已完成阶段

| 阶段 | 实验/实现 | 结果 | 证据 |
|---|---|---|---|
| A | 逐题脱敏诊断，未改检索行为 | 标准证据进入前 3；完整 `8/8, 0/2, 2/2`；无据全拒时 `5/8, 2/2, 2/2` | 问题分析 §2 |
| B | `evidence_values` 字段证据感知索引 | 完整 `7/8, 0/2, 2/2`；无据全拒时 `3/8, 2/2, 2/2` | 问题分析 §3 |
| C | Top-10 本地 Reranker 接入 | 完整 `7/8, 0/2, 2/2`；无据全拒时 `3/8, 2/2, 2/2`；无阈值可冻结 | 问题分析 §4 |
| C.1 | dense/Reranker 双排序安全诊断 | `GROUNDED-07` 证实未进完整 Top-10；两个无据题仍有高 Reranker 分数 | 问题分析 §5 |
| C.2 | Chroma 候选池 Top-10 → Top-20 | 服务已向 Reranker 提交固定 20 候选；真实固定集仍为 `7/8, 0/2, 2/2`，无阈值可冻结；公开响应仍最多 10 条，无法据此断言 `GROUNDED-07` 是否进入内部 Top-20 | 问题分析 §5.2 |

代码回归基线：P14 相关测试 `43 passed`；全量测试 `258 passed, 2 skipped, 46 warnings`；
`python -m compileall -q app tests scripts` 通过。历史 C.1 固定集运行只复用健康本机 `8000` 服务，
并已清理验收临时数据；C.2 结果和诊断边界见[问题分析台账](检索质量问题分析与改进策略.md) §5.2。

## 3. 当前任务台账

当前没有已授权的检索行为变更。后续若获得明确授权，必须重新创建对应任务并重新执行 TDD。

| 任务 | 状态 | 完成条件 |
|---|---|---|
| 保留 C.1 双排序诊断和真实结果 | 已完成 | 安全诊断可区分 dense 与 Reranker 顺序，且不泄露正文或资源标识 |
| 完成 C.1/C.2 复跑残留清理核对 | 已完成（人工补清理） | 运行器曾遗留认证会话及解析快照；按授权精确清理后 PostgreSQL、Chroma 和本地文件均为零残留 |
| 执行 C.2 Top-20 候选池实验并记录结果 | 已完成 | 配置和服务测试证明向 Reranker 提交 20 条；固定集结果已记录 |
| 完成 C.2 临时数据清理核对 | 已完成 | PostgreSQL 用户、会话、知识库、项目、文档和 Final Collection 均为零残留 |
| 维持现有检索、模型、Chunk 和阈值 | 进行中 | C.2 仅改变候选池；未经重新授权不执行其他变量 |
| 受控 Chunk 粒度调整 | 待重新授权 | 用户明确批准后，先补需求/设计变更，再执行 TDD 和固定集复验 |
| 查询表达或更换模型 | 禁止自动执行 | 必须作为独立产品/范围决策重新授权 |
| 扩展或修改评测集 | 禁止自动执行 | 必须重新冻结 AV1-P02 Ground Truth 和门槛 `1.0` |
| P12 真实 DeepSeek 质量验收 | 阻塞 | 先冻结检索阈值并通过 P14 三项门槛 |

## 4. 决策门

1. `candidate_count=10` 且标准证据不在 dense 候选中：只能记录“召回缺失”，不得自动改 Chunk。
2. 标准证据在 dense Top-10 但 Reranker 排序或分数不可分：只能记录“重排不可分”，不得自动换模型或改查询表达。
3. 候选池不足 10 条：只能记录“候选池不完整”，不得据此断言召回缺失。
4. 出现 `OUT_OF_SCOPE` 候选：停止质量优化，优先修复授权或范围校验。
5. 只有同时达到 `>=7/8`、`2/2`、`2/2`，才允许冻结独立重排阈值并启动 P12。

以上第 1～3 项产生的 Chunk 调整、查询表达、换模型和评测集修改均属于产品/范围决策，
必须重新授权，不得视为当前 Reranker 例外的自然延伸。

## 5. C2 候选方向与当前授权状态（非当前任务）

原“检索质量改进实施方案”与 C2 候选方向已在本文件合并。此处只保留方案边界和授权状态，
不产生实施授权，也不构成当前任务顺序；当前唯一任务仍由第 3 节台账定义。

| 候选方向 | 针对问题 | 当前状态 | 获授权后的最低要求 |
|---|---|---|---|
| `values` 表示 + 现有本地 Reranker 组合复验 | 验证字段值表示与重排的组合效果 | 待重新授权 | 相同确认资料重建索引；只改变一个变量；重新运行固定集并记录延迟与清理 |
| 扩大 Chroma 候选池（Top-10 → Top-20/30） | 召回缺失 | Top-20 已获授权并完成；Top-30 待重新授权 | 仅 Top-20 结果已记录；不得自动扩大到 Top-30 |
| 受控检索窗口 | 长片段可能造成召回稀释 | 待重新授权 | 先完成专项设计评审；保持 Final Chunk、定位和引用不变；同步删除、重建和回源去重 |
| 改变查询表达 | 无据候选分数不可分 | 待重新授权 | 作为独立产品/范围决策；重新声明唯一变量和可复现方式 |
| 更换 Reranker 模型 | 无据候选分数不可分 | 待重新授权 | 作为独立产品/范围决策；重新验证本机资源、延迟、分数和固定集门槛 |
| 扩展或修改固定评测集 | — | 待重新授权 | 重新冻结 AV1-P02 Ground Truth 和验收门槛 `1.0` |
| 降低验收门槛、BM25/混合检索、远程排序 | — | 明确禁止自动执行 | 不得从本方案推导实施授权 |

上述候选没有默认推荐顺序；未获得明确授权前，不启动新的索引、查询、模型或评测集实验。

## 6. C.3 第二个问题：检索质量优化方案（待明确授权）

### 6.1 问题分解

| 问题 | 已证实证据 | 直接含义 |
|---|---|---|
| 召回缺失 | C.1 中 `GROUNDED-07` 的 `candidate_count=10`，标准证据的 dense 与 Reranker 排名均为空；C.2 公开响应仍只返回 Top-10 | 至少需要先获得内部 Top-20 的安全观测，不能把公开 Top-10 缺失误判为 Top-20 召回失败或成功 |
| 无据高分不可分 | 两个无据题最高 Reranker 分数为 `0.981258`、`0.696090`；阶段 C 在宽松与拒答上限之间没有可行阈值 | 单独调高或调低 Reranker 阈值不能同时满足有据召回和无据拒答 |

### 6.2 受控执行顺序

| 步骤 | 方案 | 变量边界 | 状态 |
|---|---|---|---|
| C.3-A | 增加仅供固定集诊断的内部 Top-20 观测 | 不改变公开响应、Chroma 查询、Chunk、查询表达、模型或阈值；复用健康本机 `8000` | 待明确诊断范围授权 |
| C.3-B | 针对召回缺失做一次受控 Chunk 粒度/窗口实验 | 只改变一个 Chunk 变量；须同步 P01 Final Chunk ID、定位、重建、删除和引用验证 | 待重新授权，禁止自动执行 |
| C.3-C | 针对无据高分选择一种分离方向 | 查询表达或更换 Reranker 模型二选一；不得同时改动，也不得扩展为远程服务 | 待重新授权，禁止自动执行 |
| C.3-D | 重新标定独立 Reranker 阈值 | 只有 C.3-B/C.3-C 后固定集同时达到 `>=7/8`、`2/2`、`2/2` 才执行 | 条件成立后才可授权 |

C.3-A 是推荐的第一步，但它只增加归因证据，不代表质量已经改善。C.3-B 与 C.3-C
不能合并为“Reranker 调优”直接执行：前者会改变已冻结的 P01 定位事实，后者会突破当前
“唯一例外=本地 `BAAI/bge-reranker-base`”边界，均需要单独的产品/范围授权。

### 6.3 决策门与授权清单

- 若内部 Top-20 仍不含 `GROUNDED-07` 标准证据，只记录“召回缺失”，等待 Chunk 方案重新授权。
- 若标准证据在内部 Top-20，但无据候选仍与有据分数重叠，只记录“重排/查询不可分”，等待查询表达或模型方案重新授权。
- 不得把 Top-30、BM25、混合检索、远程排序、评测集扩展或降低门槛写成 C.3 的默认后续步骤。
- 评测集任何增删都会改变 AV1-P02 Ground Truth，必须重新冻结门槛 `1.0`。
- 未获得上述授权前，不重建正式索引、不修改查询、不改模型、不改评测集，也不进入 P12。

### 6.4 C.3 完成标准

C.3 只有在同一固定 12 题、健康本机 `8000`、真实 PostgreSQL/Chroma/BGE/Reranker 链路上，
同时满足以下条件时才可关闭：

```text
有据命中 >= 7/8
无据拒答 = 2/2
项目隔离 = 2/2
```

否则只追加安全聚合证据和失败原因，不冻结任何阈值，不进入 P12。

## 7. 获授权后的统一 TDD 流程

1. 先写并实际运行可观察行为的 RED 测试。
2. 只实现使测试通过的最小行为，保持授权范围内的接口和数据事实。
3. 相关测试通过后再重构；所有新增注释和文档字符串使用中文。
4. 运行阶段定向测试、全量 `pytest` 和 `compileall`。
5. 复用健康本机 `8000`，运行同一固定 12 题；保存安全聚合结果并清理验收数据。
6. 只有质量门槛全部通过，才更新需求/架构/接口冻结状态并进入 P12。

## 8. 维护规则

- 本文件只保留当前任务、状态和决策门；历史实验细节追加到[问题分析台账](检索质量问题分析与改进策略.md)。
- 每次真实运行必须记录日期、环境、唯一变量、聚合指标、诊断结论和清理结果。
- 真实复跑若清理失败，必须追加到问题分析台账并保持阻塞状态，不得覆盖历史通过记录或写成完整验收通过。
- 不将单文档 canary、离线 Mock、局部排名改善或测试通过写成 P14 质量通过。
- 更新本文件时同步检查 [ADR](P14-C1-双排序诊断决策.md)、README、`LEARNING_PLAN.md` 和 `AGENTS.md` 的状态与链接。
