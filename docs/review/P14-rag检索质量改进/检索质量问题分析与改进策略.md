# P14 检索质量问题分析与证据台账

## 文档状态

| 项目 | 当前值 |
|---|---|
| 文档角色 | P14 历史证据、诊断结果和因果分析台账 |
| 当前结论 | C.3-A 已证实完整 Top-20 仍有召回缺失；C.3-B 与 C.3-C 均未改善固定集，查询表达复验也未分离无据高分 |
| 更新规则 | 真实运行按日期追加；不在此维护当前任务清单 |
| 当前计划 | [P14-C2 检索质量优化方案](P14-C2-检索质量优化方案.md) |
| 决策记录 | [P14-C1 双排序诊断决策](P14-C1-双排序诊断决策.md) |

本台账区分“已证实事实”“合理推测”和“尚未验证”，不把局部成功写成质量通过。固定集门槛始终是：

```text
有据命中 >= 7/8
无据拒答 = 2/2
项目隔离 = 2/2
```

## 1. 固定集与链路

固定集包含 8 个有据问题、2 个无据问题和 2 个项目隔离问题，来自 AV1-P02 已冻结的虚构资料与
Ground Truth。正式链路为：

```text
Bearer 身份与项目授权
→ PostgreSQL 取得 CONFIRMED 且未被删除阻断的 document_id
→ BGE 查询向量
→ Chroma 按 user_id + project_id + kb_id + document_id 召回 Top-20
→ 校验候选元数据和范围
→ 本地 Reranker（阶段 C）重排
→ 按当前独立阈值筛选并返回原始 Final Chunk 引用
```

## 2. 阶段 A：纯稠密诊断（2026-08-31）

### 实验变量

- 使用同一固定 12 题和同一确认—INDEX 数据，仅增加逐题脱敏诊断。

### 环境

- 健康的本机 API、PostgreSQL、Chroma 和 512 维 BGE；未改变检索行为。

### 固定集结果

- 标准证据均进入前 3 候选：6 题第 1、1 题第 2、1 题第 3。
- 完整候选：有据 `8/8`、无据拒答 `0/2`、隔离 `2/2`。
- 拒绝全部无据候选时：有据 `5/8`、无据拒答 `2/2`、隔离 `2/2`。

### 脱敏诊断

- 逐题记录只保留稳定题号、dense 排名和距离字段，不输出正文或资源标识。

### 已证实事实

- 未出现 `OUT_OF_SCOPE` 候选。
- 正确证据通常已经进入 Chroma 候选集；阶段 A 排除了“全部正确证据未召回”和“范围过滤失效”作为主因。

### 仍属推测的内容

正确证据和错误/无据候选的 dense distance 可能不可分；这只是排序层推测。

### 对下一决策的影响

该结果支持进入阶段 B，但不支持冻结阈值或开始 P12。

## 3. 阶段 B：`evidence_values` 索引（2026-08-31）

### 实验变量

- 只把当前快照中已确认、具有定位且锚点匹配的字段值注入对应 Final Chunk 的向量输入。
- 不改变原文引用、定位、Chunk ID、项目授权或固定集 Ground Truth。

### 环境

- 复用本机 `8000` 的真实 PostgreSQL、Chroma 和 BGE 链路。

### 固定集结果

- 12 份文档获得字段上下文，覆盖 63 个 Chunk，零覆盖文档为 0。
- 完整候选：有据 `7/8`、无据拒答 `0/2`、隔离 `2/2`。
- 拒绝全部无据候选时：有据 `3/8`、无据拒答 `2/2`、隔离 `2/2`。

### 脱敏诊断

- 12 份文档获得字段上下文，覆盖 63 个 Chunk，零覆盖文档为 0。

### 已证实事实

字段映射实际生效，但该表示未通过门槛且劣于阶段 A 的 `5/8` 上限。因此不冻结默认模式或阈值。

### 仍属推测的内容

仅凭该轮结果不能证明字段值模式是召回下降的唯一原因。

### 对下一决策的影响

不自动调整 Chunk 粒度；阶段 B 当时的本地 Reranker 需单独授权，后续已在阶段 C 边界确认中获得授权。

## 4. 阶段 C：本地 Reranker 接入（2026-09-01）

### 实验变量

- 仅对授权和 Chroma 元数据校验后的 Top-10 候选使用本地 `BAAI/bge-reranker-base`。
- 不引入 BM25、混合召回、远程排序、跨项目检索或 Chunk 切分调整。
- 保留兼容 dense `score`，以独立 `reranker_score` 进行排序和阈值标定。

### 环境

- 复用真实 PostgreSQL、Chroma、512 维 BGE 和本地 Reranker 链路。

### 固定集结果

- 完整候选：有据 `7/8`、无据拒答 `0/2`、隔离 `2/2`。
- 拒绝全部无据候选时：有据 `3/8`、无据拒答 `2/2`、隔离 `2/2`。
- 不存在同时满足门槛的独立重排阈值。

### 脱敏诊断

- 本阶段仅记录聚合结果，未覆盖“标准证据是否进入完整 Top-10”的双排序归因；该缺口由 C.1 补足。

### 已证实事实

Reranker 不能在当前候选和分数分布上同时保留有据结果并拒绝无据问题。该结果本身不说明是召回
缺失还是重排不可分。

### 仍属推测的内容

召回缺失和重排不可分可能同时存在，需用同一候选池的两套排名分别验证。

### 对下一决策的影响

进入 C.1 双排序诊断；P12 仍阻塞。

## 5. 阶段 C.1：双排序诊断（2026-09-01）

### 实验变量

- 只增强固定集运行器的安全诊断，复用健康的本机 `8000` 服务。
- 不启动临时 `8002`，不改变线上检索、模型、候选池、Chunk 或阈值。

### 环境

- 健康的本机 `8000`、PostgreSQL、Chroma、512 维 BGE 和本地 Reranker。
- dense 排名由 `score = 1 - distance` 重建；Reranker 排名读取正式响应顺序。

### 固定集结果

- 聚合结果仍为有据 `7/8`、无据拒答 `0/2`、隔离 `2/2`；不存在可冻结阈值。

### 脱敏诊断

- `GROUNDED-07` 的 `candidate_count=10`，但标准证据的 dense 与 Reranker 排名均为空，直接证实
  正确证据未进入 Chroma Top-10，属于召回缺失。
- 两个无据题的最高 Reranker 分数为 `0.981258`、`0.696090`，对应同一候选的 dense distance
  为 `0.289213`、`0.334844`，说明无据候选仍无法由当前独立分数阈值全部拒绝。

### 已证实事实

- 至少一题的主问题是 Chroma 召回缺失，而不是 Reranker 将已召回证据排除。
- 无据候选的高 Reranker 分数仍是独立阻塞因素；不能仅凭修复召回就宣称 P14 通过。
- 本轮虚构账号、项目、文档和内部知识库已清理为零。

### 仍属推测的内容

- 仅凭 `GROUNDED-07` 不能推断所有有据题都需要 Chunk 调整。
- 无据题的高分是否可通过候选池或查询表达改变，尚未在授权范围内验证。

### 对下一决策的影响

- 尚未授权任何新的检索行为变更，因此没有实施 Chunk 调整、查询表达、换模型或评测集修改。
- Chunk 调整会影响 P01 冻结的定位和 Final Chunk ID；查询表达或换模型会突破当前“唯一例外=本地
  Reranker”；修改评测集会改变 AV1-P02 Ground Truth，并需重新冻结门槛 `1.0`。三者均属于产品/范围
  决策，必须重新授权。

### 5.1 C.1 复跑补充：结论复现但清理未完成（2026-09-01）

#### 实验变量

- 只复跑同一固定 12 题，不改变检索、模型、候选池、Chunk、阈值或评测集。

#### 环境

- 复用健康本机 `8000`、真实 PostgreSQL、Chroma、512 维 BGE 和本地 Reranker。
- 运行器在阈值标定失败后进入清理流程；清理主体需要当前进程直连远程 PostgreSQL。

#### 固定集结果

- 聚合结果仍为有据 `7/8`、无据拒答 `0/2`、隔离 `2/2`。
- 不存在同时满足门槛的 Reranker 阈值；拒绝全部无据候选时有据为 `3/8`，无据 `2/2`，隔离 `2/2`。

#### 脱敏诊断

- `GROUNDED-07` 的 `candidate_count=10`，标准证据的 dense 与 Reranker 排名均为空，召回缺失结论复现。
- 两个无据题的最高 Reranker 分数仍为 `0.981258`、`0.696090`，对应候选的 dense distance 为
  `0.289213`、`0.334844`。

#### 已证实事实

- 本次复跑没有改变 C.1 的检索质量结论：当前候选池和分数分布仍不能冻结阈值。
- 项目、文档和 Final Chroma Chunk 已清理为零。
- PostgreSQL 仍有 `1` 个临时 `p14be-*` 用户、`1` 个会话和 `2` 个所属知识库；运行器直连数据库时
  受本机权限限制，故本次运行不能作为“清理为零”的完整验收证据。

#### 仍属推测的内容

- 本次复跑没有提供新的召回根因证据，也不能据此判断所有有据题都需要 Chunk 调整。

#### 对下一决策的影响

- 先完成精确临时数据清理并复核 PostgreSQL、Chroma、文件均为零，再考虑新的产品/范围授权。
- 本次复跑不改变历史 C.1 记录，也不授权 Chunk 调整、查询表达、换模型或评测集修改。

## 6. 阶段 C.2：候选池扩大 Top-10 → Top-20（2026-09-01）

### 实验变量

- 仅将正式检索服务交给 Reranker 的 Chroma 候选数从固定 Top-10 改为固定 Top-20。
- 保持 `top_k` 公开响应上限 10；不改变 Chunk、查询表达、Reranker 模型、阈值或 AV1-P02 固定评测集。

### 环境

- 复用健康本机 `8000`，连接真实 PostgreSQL、Chroma、512 维 BGE 和本地
  `BAAI/bge-reranker-base`；未启动临时 `8002`。
- 通过配置与服务 TDD 证明 Chroma 请求 `n_results=20`，并将 20 条范围校验后的候选交给 Reranker。

### 固定集结果

- 同一 12 题结果仍为有据 `7/8`、无据拒答 `0/2`、项目隔离 `2/2`；拒绝全部无据候选时仍为有据
  `3/8`、无据 `2/2`、隔离 `2/2`。
- 不存在同时满足门槛的独立 Reranker 分数阈值，故不冻结阈值，也不进入 P12。
- 公开接口仍只返回最终 `top_k=10` 条；本轮安全运行器的 `candidate_count=10` 表示返回项数量，
  不能把 `GROUNDED-07` 在返回 Top-10 中缺失解释为它未进入内部 Top-20。

### 脱敏诊断

- `GROUNDED-07` 在本轮最终返回项中仍未出现标准证据；由于诊断只能读取公开 Top-10，内部 Top-20
  的 dense 排名和 Reranker 排名没有直接可见证据，不能据此判定召回已修复或仍缺失。
- 两个无据题的最高 Reranker 分数为 `0.981258`、`0.696090`，对应候选 dense distance 为
  `0.289213`、`0.334844`；高分拒答问题仍然存在。

### 已证实事实

- Top-20 候选池配置生效，且经过授权与元数据校验的候选才会进入本地 Reranker。
- 扩大候选池没有让固定集达到三项质量门槛；当前没有可冻结阈值。
- 本轮运行器清理分支曾返回失败并遗留认证会话及 12 个解析快照；随后按用户授权精确删除本次 `p14be-*`
  用户、会话和内部知识库，并核对 PostgreSQL 用户、会话、知识库、项目、文档、Final Collection 及本地
  快照文件均为零。

### 仍属推测的内容

- 仅凭公开 Top-10 返回项，无法判断标准证据是否位于内部 Top-20 的第 11～20 位。
- Top-20 未改善质量，不能推断需要 Top-30、Chunk 调整、查询表达、换模型或修改评测集；这些方向均需重新授权。

### 对下一决策的影响

- C.2 仅完成已授权的单变量候选池实验；不冻结阈值，不自动扩大候选池，不开始 P12。
- 若要获得内部 Top-20 的逐题召回证据，需另行提出不改变公开响应的安全诊断方案并重新确认范围；在此之前，
  不得把 `GROUNDED-07` 标记为“Top-20 已召回”。

## 7. 阶段 C.3-A：内部 Top-20 双排序诊断（2026-09-01）

### 实验变量

- 只增加开发环境隐藏诊断接口和固定集运行器的脱敏读取。
- 公开检索仍按 Top-20 交给本地 `BAAI/bge-reranker-base`，公开响应仍最多返回 10 条；不改变
  Chunk、查询表达、模型、阈值或 AV1-P02 Ground Truth。

### 环境

- 复用健康的本机 `8000` 服务、真实 PostgreSQL、Chroma、512 维 BGE 和本地 Reranker。
- 诊断响应只包含候选数量、dense/reranker 排名、距离、分数、标准证据布尔匹配和安全候选类别，
  不包含问题正文、文件名、路径、摘录、资源标识或 Token。

### 固定集结果

- 同一 12 题仍为有据 `7/8`、无据拒答 `0/2`、项目隔离 `2/2`；拒绝全部无据候选时有据 `3/8`、
  无据 `2/2`、隔离 `2/2`；不存在可冻结的 Reranker 分数阈值。
- 12 题的 Chroma 原始候选和范围校验后候选均为 20，证明本轮确实观测了完整 Top-20。

### 脱敏诊断

- `GROUNDED-01`、`GROUNDED-05`、`GROUNDED-07` 的标准证据在完整 Top-20 中均未出现，
  因而 dense 与 Reranker 排名均为空，直接证实召回缺失，而不是 Reranker 将已召回证据排下去。
- 其余有据题的标准证据 dense 排名为 `GROUNDED-02=8`、`GROUNDED-03=6`、`GROUNDED-04=2`、
  `GROUNDED-06=3`、`GROUNDED-08=3`；这些排名同时保留独立 Reranker 排名和分数差字段。
- 两个无据题的最高 Reranker 分数分别为 `0.981258`、`0.696090`，对应最强候选的 dense distance
  分别为 `0.289213`、`0.334844`；字段在没有候选时仍固定输出 `null`。

### 已证实事实

- Top-20 没有让固定集达到质量门槛；当前至少同时存在召回缺失与无据高分不可分两个问题。
- `GROUNDED-07` 的 C.1 Top-10 缺失不是“证据落在第 11～20 位”的情况，本轮已证明它在完整 Top-20
  仍未召回；同样的召回缺失还出现在 `GROUNDED-01` 和 `GROUNDED-05`。
- 本轮运行器首次清理因直连远程 PostgreSQL 的本机权限限制失败；随后只针对本次 `p14be-*` 范围完成
  精确清理，数据库、Final Chroma 和本地解析快照复核为零残留。

### 仍属推测的内容

- 诊断证明了召回缺失，但没有证明某一种 Chunk 粒度、窗口或字段表示一定能修复它。
- 无据高分是否能通过查询表达或更换 Reranker 分离，仍未在当前授权范围内验证。

### 对下一决策的影响

- C.3-B 召回侧变量和 C.3-C 无据分离变量必须按先后顺序分别取得产品/范围授权；不能把其中一个视为
  另一个的替代，也不能自动扩展为 Top-30、BM25、混合检索、远程排序或评测集修改。
- 阈值仍未冻结，P12 真实 DeepSeek 质量验收仍阻塞。

## 8. 阶段 C.3-B：`values` 表示 + 本地 Reranker 复验（2026-09-01）

### 实验变量

- 只将正式索引的向量上下文模式切换为 `values`，把已确认字段值加入每个 Final Chunk 的向量输入。
- 使用现有本地 `BAAI/bge-reranker-base` 对范围校验后的固定 Top-20 候选重排。
- 保持 Final Chunk 内容、Chunk ID、定位、引用、公开接口、查询表达、Reranker 模型和 AV1-P02 固定集不变。

### 环境

- 复用健康的本机 `8000`，连接真实 PostgreSQL、Chroma、512 维 BGE 和本地 Reranker；不启动临时 `8002`。
- 同一 12 题、同一批虚构确认资料重新建索引；运行器失败诊断增加安全 P95 延迟字段。

### 固定集结果

- 12 份文档、63 个 Final Chunk 全部获得 `values` 上下文，零覆盖文档为 0。
- 完整候选：有据 `7/8`、无据拒答 `0/2`、项目隔离 `2/2`。
- 拒绝全部无据候选时：有据 `3/8`、无据 `2/2`、项目隔离 `2/2`。
- P95 检索延迟为 `13105.77 ms`；不存在同时满足三项门槛的独立 Reranker 分数阈值。

### 脱敏诊断

- 所有题目的原始 Chroma 候选和范围校验后候选均为 20。
- `GROUNDED-01`、`GROUNDED-05`、`GROUNDED-07` 的标准证据在完整 Top-20 中仍未出现，dense 与 Reranker 排名均为空。
- 其余有据题的标准证据 dense 排名为 `GROUNDED-02=8`、`GROUNDED-03=6`、`GROUNDED-04=2`、
  `GROUNDED-06=3`、`GROUNDED-08=3`；Reranker 排名和分数差继续独立保留。
- 两个无据题的最高 Reranker 分数为 `0.981258`、`0.696090`，对应最强候选的 dense distance 为
  `0.289213`、`0.334844`。

### 已证实事实

- `values` 表示在真实索引链路中生效，覆盖 63/63 个 Chunk，但没有改善固定集的三项质量结果。
- 完整 Top-20 仍缺少上述三题标准证据，因此本轮没有证据表明问题只是“证据位于第 11～20 位”。
- 本轮不改变 Final Chunk、定位、引用、公开响应或模型；临时账号、会话、知识库、项目、文档、Final Chroma
  和解析快照均已精确清理为零。

### 仍属推测的内容

- 单次 `values` 表示复验不能证明其他向量表示、Chunk 粒度或窗口一定能修复召回缺失。
- 无据题高分是否可通过查询表达或更换 Reranker 分离，仍未验证。

### 对下一决策的影响

- C.3-B 已完成但质量门槛未通过；召回缺失仍成立，无据高分仍是独立阻塞因素。
- C.3-C 必须作为独立产品/范围决策重新授权并选择查询表达或模型中的一个；在此之前不执行新的查询、模型、
  Chunk、窗口或评测集变更，不冻结阈值，也不进入 P12。

## 9. 阶段 C.3-C：固定查询表达 + 本地 Reranker 复验（2026-09-01）

### 实验变量

- 只把原始问题规范化为固定表达 `档案证据检索问题：{query}`。
- 该表达同时传给 BGE 查询侧（外层仍保留既有 `为这个句子生成表示以用于检索相关文章：` 指令）和本地
  `BAAI/bge-reranker-base`；不使用 LLM 改写、查询扩展或随机模板。
- 保持 Chroma 固定 Top-20、Final Chunk、定位、引用、公开响应和 Reranker 模型不变。

### 环境

- 复用健康的本机 `8000`，连接真实 PostgreSQL、Chroma、512 维 BGE 和本地 Reranker；未启动 `8002`。
- 使用当前可完成运行器确认计数的 `.env` `evidence_values` 索引基线；这与此前文档将 C.3-B 简写为
  `values` 的计划命名存在差异，本轮没有把该配置差异作为 C.3-C 变量。
- 运行器自动清理失败后，只针对本轮 `p14be-*` 范围精确清理；最终 PostgreSQL 用户、会话、知识库、项目、
  文档、解析快照和 Final Chroma 均为零。

### 固定集结果

- 12 份文档、63 个 Final Chunk 全部获得字段上下文，零覆盖文档为 0。
- 完整候选：有据 `7/8`、无据拒答 `0/2`、项目隔离 `2/2`。
- 拒绝全部无据候选时：有据 `3/8`、无据 `2/2`、项目隔离 `2/2`。
- 检索 P95 为 `3791.19 ms`；不存在同时满足三项门槛的独立 Reranker 阈值。

### 脱敏诊断

- 所有 12 题的原始 Chroma 候选和范围校验后候选均为 20。
- `GROUNDED-01`、`GROUNDED-05`、`GROUNDED-07` 的标准证据仍未进入完整 Top-20，dense 与 Reranker 排名均为空。
- 其余有据题的标准证据 dense 排名为 `GROUNDED-02=9`、`GROUNDED-03=6`、`GROUNDED-04=2`、
  `GROUNDED-06=5`、`GROUNDED-08=2`；Reranker 排名分别为 `8`、`1`、`1`、`1`、`1`，独立分数差字段继续保留。
- 两个无据题的最高 Reranker 分数为 `0.973935`、`0.707142`，对应最强候选 dense distance 为
  `0.287029`、`0.311391`；安全诊断未输出问题正文、文件名、路径、摘录、资源标识或 Token。

### 已证实事实

- 固定查询表达没有改变 C.3-B 实际基线的三项聚合结果，也没有消除 `GROUNDED-01/05/07` 的召回缺失。
- 两个无据题仍出现高 Reranker 分数；仅凭该模板不能冻结独立阈值。
- 查询表达变量已完成一次受控复验；本轮没有更换模型、调整 Chunk、扩大候选池或修改固定集。

### 仍属推测的内容

- 单一短模板未改善结果，不能证明所有查询表达都无效，也不能证明更换模型必然有效。
- 召回缺失的修复方向仍不能从本轮结果推导，需新的产品/范围授权。

### 对下一决策的影响

- C.3-C 已完成但质量门槛未通过；C.3-D 阈值标定条件不成立，P12 继续阻塞。
- 不得自动重复查询表达、换模型、调整 Chunk/窗口、扩大到 Top-30、引入 BM25/混合检索、远程排序或修改
  AV1-P02 Ground Truth；这些均需重新授权。

## 10. 阶段 C.3：检索质量优化的决策状态（C.3-A 实施前快照，2026-09-01）

### 已证实事实

- `GROUNDED-07` 在 C.1 的完整 Top-10 中没有标准证据，说明至少存在召回缺失。
- C.2 只扩大了服务提交给 Reranker 的候选数，公开响应仍最多返回 Top-10，因此不能从返回项证明
  标准证据是否位于内部 Top-20。
- 两个无据题的最高 Reranker 分数为 `0.981258`、`0.696090`，与有据题错误候选的高分重叠；
  当前分数没有可冻结的单一阈值。

### 授权状态

- C.3 的具体执行顺序、变量边界和完成标准只由
  [P14-C2 检索质量优化方案](P14-C2-检索质量优化方案.md)第 6 节维护；本台账不再复制任务顺序。
- 在 C.3-A 实施前，内部 Top-20 安全诊断、召回侧变量、无据分离变量和阈值标定均未获得新的实施授权。
- Top-30、BM25、混合检索、远程排序、修改或扩展 AV1-P02 Ground Truth、降低门槛均明确禁止自动执行。
  任何评测集修改都必须重新冻结门槛 `1.0`。

### 对下一决策的影响

在用户明确授权具体变量前，不重建正式索引、不改变查询或模型、不修改评测集、不冻结阈值，也不
开始 P12。授权后的真实实验无论成败，都在本台账按“实验变量、环境、固定集结果、脱敏诊断、已证实
事实、仍属推测、决策影响”追加记录，不把局部改善写成 P14 通过。

## 11. 候选方向及授权状态（历史快照）

本节记录 2026-09-01 的授权边界，不是当前任务来源，也不随实施计划滚动更新；当前状态以
[P14-C2 检索质量优化方案](P14-C2-检索质量优化方案.md)第 3、5、6 节为准。

| 方向 | 当前状态 | 约束 |
|---|---|---|
| C.3-A 内部 Top-20 安全诊断 | 已实施并完成复验 | 只增加固定集观测，不改变公开响应、检索、模型、Chunk 或阈值 |
| C.3-B `values` 表示 + 本地 Reranker 复验 | 已完成，质量门槛未通过 | 只改变索引向量表示；Final Chunk、定位、引用、公开接口和模型保持不变 |
| 受控 Chunk 粒度调整 | 待重新授权 | 影响 P01 定位和 Final Chunk ID |
| 改变查询表达 | 待重新授权 | 突破当前本地 Reranker 唯一例外 |
| 更换 Reranker 模型 | 待重新授权 | 突破当前本地 Reranker 唯一例外 |
| 扩展或修改固定评测集 | 待重新授权 | 修改 AV1-P02 Ground Truth，门槛需重新冻结为 `1.0` |
| 扩大 Chroma 候选池至 Top-20 | 已获授权并完成 C.2；质量门槛仍未通过 | 不得自动扩大至 Top-30；公开 Top-10 返回仍不足以提供内部 Top-20 逐题召回证据 |
| 降低验收门槛 | 明确禁止自动执行 | 会破坏已有质量基线 |
| P12 真实 DeepSeek 验收 | 阻塞 | 需先通过 P14 三项检索门槛 |

本表只保留历史授权快照，不维护执行任务或步骤顺序。

## 12. 后续记录模板

每次新的真实运行追加以下字段，禁止覆盖历史记录：

```text
日期：
阶段/唯一变量：
环境：是否复用健康本机 8000
固定集版本：AV1-P02 版本与 Ground Truth 状态
索引上下文摘要：文档数、Chunk 数、零覆盖数、向量表示模式
延迟：检索 P95（毫秒）
结果：有据 / 无据 / 隔离
双排序诊断：仅安全数值、排名、分数和候选类别
已证实事实：
未证实推测：
决策影响：
清理核对：PostgreSQL、Chroma、文件、账号/知识库
```
