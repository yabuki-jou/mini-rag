# 实施前验证与冻结清单(v3,最终版)

> **实现状态更新（2026-08-27）：** 向量后端已按项目决策迁移为 Chroma；P09 真实确认—INDEX/取消确认
> 清理、P11 单文档检索 canary 和 P10～P13 实现切片已完成。门槛 2.2 的固定 12 问题阈值/召回质量、
> 门槛 3.1 的真实跨存储恢复和门槛 3.2 的真实 DeepSeek 引用质量仍未通过验收。

> 技术值按**三类门槛**分批冻结,每项明确"不阻塞什么、阻塞什么":
> - **门槛 1**:实施计划前(不阻塞拆任务,可并行创建)
> - **门槛 2**:第一条纵向链路后(上传→解析→Embedding→Chroma→检索,不含问答)
> - **门槛 3**:相关纵向切片实现后(跨存储恢复切片 / 问答切片)

---

## 门槛 1:实施计划前(不阻塞拆任务,可在计划拆好后并行创建/验证)

### 1.0 虚构验收资料与人工标注

| 属性 | 内容 |
|---|---|
| 冻结对象 | 验收资料文件(覆盖 6 个资料分类,每类 2~3 份,共 12~18 份)、每份的人工标注(资料类型、可识别字段、标准证据)、12 个固定问答问题(8 有据 + 2 无据 + 2 隔离)及其标准答案 |
| 文档引用 | 需求 §12.1~§12.3、§16(TODO-EVALUATION) |
| 冻结条件 | 所有资料为虚构/脱敏文本;PDF/DOCX/TXT/MD 格式覆盖 Parser 全部适配器;人工标注作为 AI 质量评测的 Ground Truth;问答问题覆盖检索命中、拒答、隔离三类场景 |
| 阻塞 | 不阻塞实施计划生成;阻塞检索与问答质量验收(门槛 2.2、3.2) |

**冻结结果（AV1-P02，2026-08-06）**：已冻结可复现生成器
`scripts/generate_archive_v1_eval_data.py` 与本地资料目录 `tests/pytest_docs/`（Git 忽略）。
该目录包含 12 份正常资料、3 份异常资料、字段/证据 Ground Truth 与 12 个固定问题；
`tests/test_archive_v1_evaluation_data.py` 已验证覆盖、证据、重复哈希、扫描 PDF 和问题
项目范围。该项只满足资料准备门槛，不构成模型、检索或问答质量结论。

### 1.1 Parser 定位规则与有效文本阈值

| 属性 | 内容 |
|---|---|
| 冻结对象 | "有效文本"字符数阈值、空白归一化策略、DOCX 段落序号生成规则(表格/编号列表处理)、TXT/MD 换行归一化规则与行号范围稳定化实现 |
| 文档引用 | 需求 §15(假设与限制)、§16(TODO-PARSER);架构 §8.1~§8.2、§18(TODO-PARSER);数据库 §6.4 |
| 冻结条件 | 运行解析验证脚本(可处于 WIP 分支),确认:在同一原文件、固定解码与换行归一化规则下,定位结果(页码/段落序号/行号范围)可重复生成并与保存的解析快照一致;扫描 PDF 正确识别为无文本并返回 PARSE_FAILED;"有效文本"阈值能过滤空壳文档 |
| 阻塞 | 不阻塞实施计划与 Parser 验证代码;阻塞以下动作的正式产物:创建 Alembic 迁移(如 snapshot 表)、注册正式归档 API 路由(与解析相关的接口)、编写 Parser 验收测试 |
| 冻结后写入 | Parser 设计说明(或解析器实现文档)、验收说明中的定位契约 |

**冻结结果（AV1-P01，2026-08-06）**：已通过隔离验证并记录于
`docs/智慧档案V1解析器设计.md`。固定 `archive-v1-parser-v1`、UTF-8/UTF-8 BOM 解码、
CRLF/CR→LF、连续空白压缩、DOCX 非空逻辑块（段落/列表项/表格行）序号、TXT/MD 原始非空
行号、DOCX 前 50 字辅助定位、最小 20/最大 1,000,000 个归一化字符，以及固定 JSON
序列化 SHA-256 `snapshot_hash`。P01 只解除本项对正式迁移/解析 API/Parser 验收测试的
规则冻结阻塞；不代表这些正式产物已实现。

### 1.2 契约级参数(在设计阶段已确认,无需验证)

| 参数 | 值 | 依据 |
|---|---|---|
| 列表分页 | `page=1`、`page_size=20`、最大 `100` | API 补丁 rev2 §1.1,用户已确认 |
| Top-K | `top_k=5`、最大 `10` | API 补丁 rev2 §1.3,用户已确认 |
| 问题长度 | `1..2000` 字符 | API 补丁 rev2 §1.3,用户已确认 |
| confirmed ID 过滤上限 | 每个项目 ≤ 100 个(由 BR-011 每项目 100 份未删除文档直接推导) | 无需实验,属于准入门槛已满足 |
| Idempotency-Key | 不启用 | API 补丁 rev2 §1.2,用户已确认 |

---

## 门槛 2:第一条纵向链路后(上传→解析→Embedding→Chroma→检索,不含问答)

> 门槛 2 阻塞:min_relevance_score 冻结(影响问答实现)、正式 API 注册(FR-039 检索接口)、检索验收测试。

### 2.1 BGE 维度 + Chroma Collection + 检索性能基线

| 属性 | 内容 |
|---|---|
| 冻结对象 | BGE 模型输出向量维度、`archive_final_chunks` Collection 配置、向量维度、距离度量和检索参数 |
| 文档引用 | 架构 §9.3、§18(TODO-VECTOR);数据库 §9 |
| 冻结条件 | 使用实际 BGE 模型跑通一条完整检索链路(上传→解析→快照→Embedding→写入 Chroma→检索);确认维度匹配、Collection 配置成功、confirmed ID 过滤正确工作 |
| 性能基线(记录,非通过/失败条件) | 在本地 Docker Chroma、单项目 20~100 份已确认文档的数据规模下,记录单次检索 P95 延迟 Y ms 作为本地演示基线;此基线不用于判定通过/失败,仅用于验收报告中的可复现性说明 |
| 冻结后写入 | Chroma 初始化/配置、数据库设计 §9 字段定稿、验收报告的检索性能基线章节 |

### 2.2 min_relevance_score + Chroma 度量类型与分数方向

| 属性 | 内容 |
|---|---|
| 冻结对象 | `min_relevance_score` 数值、Chroma 距离度量类型(当前为 `cosine`)、分数方向(距离越小越相关，转换分数越大越相关) |
| 文档引用 | API 补丁 `design-baseline-patch-2-rev2` §1.3、§6.2 |
| 冻结条件 | 使用已创建的验收资料(项 1.0)与 12 个固定问答用例,在 Parser/BGE/Chroma 检索管道跑通后标定;标定目标:8 个有据问题中**至少 7 个检索结果包含标准证据**、2 个无依据问题**全部检索为空(过滤后无候选)**、2 个隔离问题**不返回其他项目数据**(均与需求 12.3 口径一致,仅验证检索层,不涉及问答生成) |
| 阻塞 | min_relevance_score 冻结前,不应将问答接口(FR-039 `archive-questions`)视为实现完成或进入验收 |
| 冻结后写入 | 问答服务固定配置、实施计划中 FR-039 的验收说明 |

---

## 门槛 3:相关纵向切片实现后

> 每个子项独立,不互相阻塞。

### 3.1 跨存储恢复算法(确认/删除纵向切片后)

| 属性 | 内容 |
|---|---|
| 冻结对象 | 确认/删除时跨存储(PostgreSQL / 文件系统 / Chroma)部分失败的具体补偿步骤、ArchiveOperation 的 `last_completed_step` 枚举值、恢复入口(复用同一 API) |
| 文档引用 | 架构 §12.1、§12.2、§18(TODO-IMPLEMENTATION);数据库 §6.9、§10 |
| 冻结条件 | 在确认与删除的跨存储纵向切片实现后(非"全部业务闭环后"),以该切片代码在集成测试环境中模拟故障场景:PostgreSQL 成功但 Chroma 失败、文件删除成功但 PostgreSQL 失败等;验证恢复流程可重入、不重复写数据、visibility_blocking 不泄露、审计不重复写入 |
| 阻塞 | 不阻塞实施计划生成;阻塞 FR-036(确认)、FR-040(删除)的集成验收 |

### 3.2 完整问答链路引用正确率(问答切片实现后)

| 属性 | 内容 |
|---|---|
| 冻结对象 | 最终回答引用正确率 100%(需求 12.3:任一错误引用均不通过) |
| 文档引用 | 需求 §12.3 |
| 冻结条件 | DeepSeek 问答切片实现后,在完整管道(检索→Prompt→DeepSeek→引用解析)下运行 12 个固定问答用例,验证:每个回答的引用均指向已确认档案、文件名与定位正确、无伪造引用、无待确认/待重新确认/其他项目数据 |
| 阻塞 | 阻塞 FR-039(问答)的集成验收 |

---

## 门槛总览

| 门槛 | 任务 | 不阻塞 | 阻塞 |
|---|---|---|---|
| 1 | 1.0 验收资料 | 实施计划生成 | 门槛 2.2、3.2 的验收 |
| 1 | 1.1 Parser 规则 | 实施计划、Parser 验证代码 | 正式迁移、归档 API、Parser 验收测试 |
| 1 | 1.2 契约参数 | — | —(已确认) |
| 2 | 2.1 BGE+Chroma | 实施计划生成 | 正式 API 注册(FR-039 检索)、检索验收 |
| 2 | 2.2 检索阈值标定 | 实施计划生成 | 问答实现(FR-039 `archive-questions`)、检索验收 |
| 3 | 3.1 跨存储恢复 | 实施计划生成、其他 FR 验收 | FR-036、FR-040 集成验收 |
| 3 | 3.2 问答引用正确率 | 实施计划生成、检索验收 | FR-039 `archive-questions` 集成验收 |

---

*本清单 2026-08-06 v3,取代前版。门槛定义收紧点:① 1.1 不阻塞 Parser 验证代码;② 2.1 性能基线改为记录值不作通过/失败;③ 2.2 不验证引用正确率(移至 3.2);④ 3.1 修正为"纵向切片后"而非"完整业务闭环后"。*

---

## 附录:评审意见追溯(v1→v2→v3)

> 每项冻结条件/门槛定义的最终形态,均经过多轮评审修正。以下按评审轮次追溯修改来源。

### v1→v2(2026-08-06,首次评审 5 点)

| # | 评审意见 | 来源 | v2 修改 |
|---|---|---|---|
| A1 | 执行顺序存在依赖矛盾:阈值标定(原项 1)依赖验收资料(原项 5),但顺序把项 5 放在项 1 之后 | 第一轮评审 | 修正为 0→1→2→3→4;验收资料前置为 1.0 |
| A2 | DOCX/TXT 定位冻结条件"行号与外部编辑器复现一致"与需求矛盾:需求只承诺原始上传文件定位 | 第一轮评审 | 改为"在同一原文件、固定解码与换行归一化规则下,定位结果可重复生成并与保存的快照一致" |
| A3 | "检索延迟可接受"不可判定,缺少测量方式和目标 | 第一轮评审 | 增加 P95 延迟测量条件(本地 Docker、单项目 20~100 份文档) |
| A4 | confirmed ID 过滤长度可直接由 BR-011(每项目 100 份)推导,无需实验 | 第一轮评审 | 从待验证项移除,放入契约级参数(1.2),值直接标注"≤ 100" |
| A5 | 跨存储恢复算法不能作为"正式实现前"全部冻结的前置门槛;应是实施计划中的早期纵向切片 | 第一轮评审 | 降为门槛 3(集成测试阶段),不阻塞实施计划生成 |

### v2→v3(2026-08-06,收紧 4 点)

| # | 评审意见 | 来源 | v3 修改 |
|---|---|---|---|
| B1 | 门槛 1.1 不能要求"第一条代码写出前冻结":Parser 的有效文本阈值、DOCX 表格和行号规则需纵向切片实测 | 第二轮评审 | 阻塞列改为"不阻塞实施计划与 Parser 验证代码;阻塞正式迁移、归档 API、Parser 验收测试" |
| B2 | 门槛 2.1"P95 不超过 X ms,X 由实测确定"仍是伪门槛:先测多少 X 就设多少 | 第二轮评审 | 新增"性能基线(记录,非通过/失败条件)"小节;P95 = Y ms 仅作为本地演示基线记录,不用于判定通过/失败 |
| B3 | 门槛 2.2"引用正确率 100%"依赖 DeepSeek 问答服务,但门槛 2 的管道描述只到"检索",不含问答 | 第二轮评审 | 门槛 2.2 拆分为仅验证检索层(命中/拒答/隔离);引用正确率 100% 移至新门槛 3.2(依赖问答切片实现) |
| B4 | 门槛 3 标题写"完整业务闭环跑通后",正文又说"早期纵向切片验证任务",时间矛盾 | 第二轮评审 | 门槛 3.1 改为"确认/删除跨存储纵向切片实现后(非全部业务闭环后)",阻塞 FR-036/040 集成验收;3.2 独立不互卡 |

### 已确认部分(两轮均通过,未修改)

- 验收资料前置(1.0)
- 原文件定位契约(1.1 冻结条件中的"同一原文件、固定规则下可重复生成")
- confirmed ID ≤ 100 直接推导(1.2 契约参数)
- 阈值标定依赖顺序(1.0→1.1→2.1→2.2)
- 恢复算法不阻塞实施计划生成(3.1)
- 三类门槛分层结构
