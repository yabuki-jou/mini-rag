# 项目素材评测用例稳定性修正实施计划

## 目标

修正 `archive-question-project-grounded.json` 中把易变阶段进度固化为长期问答事实的 `PROJECT-GROUNDED-04`。当前用例仍回答“下一步 FR-039”，并引用 `handoff.md` 的旧行号；实际阶段已经进入 FR-040，因此当前全量回归失败。

## 实现边界

1. 保留四条有据问题、Top-8 注入结构、现有 evaluator 和其他三条用例不变。
2. 将第四题改为基于已确认设计文档的稳定 FR-040 删除恢复事实，引用 `docs/design/接口设计.md` 中删除与审计章节的逐字内容。
3. 问题和期望答案应验证：外部删除失败返回稳定错误、文档退出正式范围、同一 DELETE 可恢复，成功只保留脱敏审计。不得继续询问“下一步是什么”。
4. 候选 `filename`、定位行号、摘录、`source_document` 和 `expected_evidence` 必须互相一致；不得改写原文冒充证据。
5. 不改生产 RAG、Prompt、质量门或测试断言。

## TDD 与验证

- RED 已由主 Agent 实际取得：全量测试中 `test_project_grounded_candidates_trace_to_declared_source_lines` 失败，且人工核对发现问题和答案也已过时。
- GREEN 后运行 `tests/pixie_qa/archive_v1_p14/test_project_grounded_materials.py`。
- 主 Agent最终运行完整后端测试与 `compileall`。
