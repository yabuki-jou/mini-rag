# FR-042 提交范围审查

## 结论

有条件通过。FR-042 后端和 Vue 改动已具备提交条件，但后端工作区有两份不属于本期且会削弱既有安全
约束的修改，必须保持未暂存状态。

## 必须排除

- `scripts/policy_collection_embedding_rebuild.py`
- `tests/scripts/test_policy_collection_embedding_rebuild.py`

这组 diff 会撤销既有迁移脚本安全收口：只读预检重新使用 `get_or_create_collection`、PostgreSQL
计数重新加载完整行、维度探针吞掉全部异常、canary 不再核对精确 ID、失败输出重新暴露底层异常，且
相应安全测试被删除。它与 FR-042 无依赖，本轮不提交、不修改、不回滚。

## 后端提交拆分

1. 当前用户资料前置：`app/routers/auth.py`、`tests/routers/test_auth.py`。这是 Vue 既有显示名功能，早于
   FR-042，应独立提交。
2. FR-042 功能：Archive Agent、共享 Checkpoint、会话范围模型与迁移、四端点、目录/证据/公共判定、
   项目删除联动、模型超时，以及对应确定性测试。
3. FR-042 评测资产：`evals/archive/` 的助手扩展、根级 `pixie_qa/` 非忽略资产、三个 FR-042 评测脚本
   和对应测试。不得提交 `pixie_qa/results/`。
4. FR-042 文档：需求、架构、数据库、接口、实施计划、评审、验收、决策、学习计划与 handoff。

实际暂存必须使用逐路径清单，禁止 `git add -A` 或目录根级通配暂存。

## Vue 提交范围

Vue 当前 12 个修改/新增文件全部属于 FR-042：类型、四端点客户端、Store、Agent 面板、路由/侧栏/工作区
接入、测试和 README。可作为一个独立提交；`dist/` 与 TypeScript 构建缓存保持忽略。

## 当前验证

- 后端全量：`601 passed, 2 skipped, 207 warnings`；`compileall` 通过。
- Vue 全量：`130 passed`；类型检查与生产构建通过。
- 两仓库 `git diff --check` 通过，仅有既有 CRLF 提示。
- 后端已按清单完成当前用户资料、FR-042 功能、评测资产和本文档四个本地提交；两个排除文件保持未暂存。
  Vue 仍待独立提交，两个仓库均尚未推送。
