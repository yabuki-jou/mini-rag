# FR-039/FR-042 世界银行贷款补充召回：真实检索验收

> 阅读提示：本文按日期追加真实检索、回答评测、TDD 和错误定位记录。各阶段中的“当前状态/下一步”只对该段记录时点有效；应继续读到最后一个带日期的阶段，并以[当前交接快照](../../stage/handoff.md)确认现在的进度和授权。检索覆盖、答案事实性、冻结目标命中、扩展来源支持和引用有效性是不同指标，不互相替代。

## 范围与结论

2026-09-25 按已确认实施计划 §5.2，在独立 PostgreSQL Schema、Chroma Database/Collection、文件目录和 Checkpoint 路径中，重新上传、解析、确认并索引 P153548 四份固定 PDF 与 Belarus Snapshot 一份扩展 PDF。来源哈希先经清单复核。本轮只运行真实 Embedding、Chroma 检索和本地 CrossEncoder，不调用 DeepSeek（`model_calls=0`）。

**§5.2 检索门通过**：九题的正式回答候选服务 Top-8 均与同一候选池上的诊断排序一致；`LUSHAN-01` 的冻结目标第 16 页按公开证据覆盖口径进入 Top-8（第 3），`LUSHAN-02/03` 的支持证据分别为第 1/5。此结论本身不代表答案事实性、最终引用或无据拒答通过；这些已在后续 §5.3 及扩展来源评测中分别检查，结果见本报告后续阶段和[扩展来源回答/引用评测计划](P153548扩展来源回答引用评测计划.md)。

## 本次观测

| 案例 | 补充查询 | 候选并集 | 关键结果 |
| --- | --- | ---: | --- |
| LUSHAN-01 | 固定裸词 `IBRD IDA` | 54 | 补充表达重排下冻结目标页公开覆盖第 3；原问题重排第 21；精确 Chunk 摘录匹配未命中 |
| LUSHAN-02 / 03 | 无 | 各 30 | 公开支持证据分别在 Top-8 第 1 / 5 |
| LUSHAN-04 | 无 | 30 | 记录为无答案支持型检索案例；是否正确拒答未测 |
| WB-REV-LOAN-01 | 固定裸词 `IBRD IDA` | 20 | Snapshot 第 16 页原问题/补充表达重排第 1/7；第 7 页第 2/17。第 7 页是项目背景，不与贷款明细页混同 |
| WB-CONTEXT-NONLOAN-01 / WB-ENTITY-NEG-01 / WB-UNSUPPORTED-DRAW-01 | 无 | 各 20 | 非触发题保持单路；后两题不把背景页当作答案支持 |
| WB-UNSUPPORTED-DRAW-GATED-01 | 固定裸词 `IBRD IDA` | 20 | 触发双路，第 16 页补充表达重排第 7；候选不支持“完工时实际提款额”，拒答待模型门验证 |

`LUSHAN-01` 五个已人工核实金额支持页在补充表达重排下的名次：2016 appraisal 第 1/16 页分别为第 9/3，2022 restructuring 第 4 页为第 7，2024 completion report 与 completion review 第 1 页分别为第 8/1；Top-8 覆盖 **4/5**。这只是 DEC-024 扩展来源诊断，不是完整召回率或冻结门分数。Belarus 也是独立扩展来源，不并入 P153548 冻结评分，亦非“完全不含 IBRD/IDA”的严格反向资料。

本次 `LUSHAN-01` 正式检索服务耗时 **11.717 秒**，54 候选的补充表达诊断重排 **11.526 秒**；同池原问题诊断重排另耗 **14.254 秒**。两种诊断排序与正式服务调用分别计时，不能相加后称为生产单次耗时。每题只运行一次，无预热重复、分位数或稳定延迟预算；不同资料池之间也不作吞吐推断。

## 证据与清理

- 脱敏逐候选结果：`tests/pytest_docs/public_projects/world_bank_belarus_m6/acceptance/retrieval-only-710b22dc3116.json`（Git 忽略；只记录候选键、来源页、排序与耗时，不含原文正文）。
- 运行器定向测试 `17 passed`；真实运行的九题均有 `production_top8.matches_reconstructed=true`，报告的 `cleanup` 四项均为 `true`。
- 停止 API 后再次确认专用 Schema、Chroma Database 和临时运行目录均不存在；Checkpoint 位于已删除的临时运行目录中。保留上述脱敏 JSON，不保留临时业务资源。日志清理的复核与补救见下文。
- 本次没有修改冻结资料或评分器，也没有运行 Pixie。§5.3 最多 45 次 DeepSeek HTTP 请求尝试已获授权；首次隔离启动在模型调用前失败（0 次模型请求），修正前置校验后完成一次 10 样本批次，实际发出 20 次请求。结果未达质量门，且运行器内层清理报告异常；具体证据与边界见下文。另行 Pixie 批次最坏 9 次，尚未获授权。

### 日志清理复核

验收编排曾将 `LOG_FILE` 基准路径设在系统临时目录，并只检查该基准文件是否不存在。复核 `app/core/logging.py::build_dated_log_path` 后发现真实日志按基准路径的**父目录**写入 `年/月/日期.log`，不沿用随机文件名前缀；因此原先“专用日志已清理”的检查没有覆盖实际文件。按文件创建时间和大小核对本次临时日志后，已删除该精确文件并复查不存在；未读取或打印日志正文。后续运行改用独立随机日志父目录，待进程退出后只清理该已校验的目录。

### §5.3 首次启动失败与定位

- 2026-09-25 首次启动已获授权的隔离答案批次后，运行器返回失败；答案结果文件不存在。外层在退出后分别核验随机 PostgreSQL Schema、Chroma Database、运行目录和专用日志目录均已删除。
- 沿 `run_answer_acceptance()` 控制流定位：`_validate_retrieval_evidence()` 在创建进程内 FastAPI 客户端、进入样本循环之前执行，因此本次没有调用 DeepSeek。其失败条件要求 `verify_source_manifests()` 的返回值包含 `source_manifest_checks.belarus_source_sha256_verified`；但该验证函数实际返回数据集、来源清单、标签和本地路径，并在返回前直接完成 PDF 哈希校验，不提供该派生字段。该不匹配使有效来源在启动门被错误拒绝。
- 外层只读预检已独立确认固定来源哈希、§5.2 `model_calls=0` 和四层清理均通过；该次启动没有答案结果可供质量判定。修正方式是依赖 `verify_source_manifests()` 已执行的直接哈希校验，不再要求其不存在的派生字段，并用其真实返回结构新增/调整回归测试；修正及复验结果见下节。

### §5.3 第二次隔离运行：答案/引用未通过及定位

- 修正来源验证契约并完成复验后，按原授权运行一次固定十样本批次；未重试任何样本。实际 DeepSeek HTTP 请求 **20/45**：FR-039 5 次、FR-042 15 次（Graph 决策 10 次、证据判定 5 次），20 次均成功返回且没有应用层重试。脱敏结果保存在 Git 忽略目录 `tests/pytest_docs/public_projects/world_bank_belarus_m6/acceptance/answer-acceptance-584fc6a9bc88.json`，不含原始回答正文。
- 总质量结果 **3/10 通过**。通过项为 FR-039 的 `LUSHAN-04` 与 `WB-UNSUPPORTED-DRAW-GATED-01`，以及 FR-042 的 `LUSHAN-04`；三项均为固定拒答且无引用。其余 FR-039、FR-042 各三轮 `LUSHAN-01` 均返回 `ANSWERED` 并命中金额标记，但六条引用定位的冻结标准摘录支持均为 `false`。FR-039 引用定位分别落在 completion report 第 1 页（1 次）及 appraisal 第 5 页（2 次）；FR-042 三次均落在 appraisal 第 1 页。因此本轮不能证明金额回答有据，也不能把问题归因为模型事实错误或引用序列化错误：原始回答未保存，无法再作这一区分。
- FR-042 的 `LUSHAN-01` 三轮和扩展无据题 `WB-UNSUPPORTED-DRAW-GATED-01` 均记录 `gate_triggered=false`，与方案预期不符。只读检查确认两类固定问题都同时满足生产门控的“世界银行指称 + 贷款意图”正则。沿实际调用链定位到 `app/agents/archive/graph.py`：工具参数经 `_ArchiveEvidenceToolInput.model_validate(...).model_dump(mode="python")` 后传给工具；针对真实 `_ArchiveEvidenceToolInput` 的本地无网络复现显示，`state.messages[0]` 中的 `HumanMessage` 在 `model_dump` 后变成普通 `dict`。`app/agents/tools/archive_tools.py::_current_gate_question` 仅接受 `HumanMessage` 实例，因此返回空字符串，补充召回门关闭。现有工具单测直接调用 `evidence_tool.func` 并传原始 `HumanMessage`，绕过该序列化边界；Graph 测试又以 Mock 检索服务隔离检索，故没有覆盖真实转换。这是“门未触发”的已验证代码原因，不是题面缺词。
- FR-042 的 `LUSHAN-01` 三轮和扩展无据题 `WB-UNSUPPORTED-DRAW-GATED-01` 均记录 `gate_triggered=false`，与方案预期不符。只读检查确认两类固定问题都同时满足生产门控的“世界银行指称 + 贷款意图”正则。沿实际调用链定位到 `app/agents/archive/graph.py`：工具参数经 `_ArchiveEvidenceToolInput.model_validate(...).model_dump(mode="python")` 后传给工具；针对真实 `_ArchiveEvidenceToolInput` 的本地无网络复现显示，`state.messages[0]` 中的 `HumanMessage` 在 `model_dump` 后变成普通 `dict`。`app/agents/tools/archive_tools.py::_current_gate_question` 仅接受 `HumanMessage` 实例，因此返回空字符串，补充召回门关闭。现有工具单测直接调用 `evidence_tool.func` 并传原始 `HumanMessage`，绕过该序列化边界；Graph 测试又以 Mock 检索服务隔离检索，故没有覆盖真实转换。这是“门未触发”的已验证代码原因，不是题面缺词。
- FR-042 的候选映射指标本轮无法独立判定：脱敏元数据中三次引用与候选的文件/页定位一致（均为 appraisal 第 1 页），但结果没有保存可复核的摘录对应关系。沿验收器输出反查 `CandidateTrace.assess_citations()`，发现旧 `mapped_to_candidate` 实际是 `mapped and tool_called and gate_matched`；本轮 `gate_matched=false` 会强制该字段为 `false`，因此旧结果不能证明候选映射失败，也无法事后恢复映射真值。
- 运行器记录 `cleanup_verified=false`、`cleanup_error_type=AcceptanceError`，没有留下安全步骤码或底层异常。`_cleanup_and_verify()` 将多个清理步骤的异常合并成通用 `AcceptanceError`，答案运行器仅保存异常类型，所以内层失败的具体清理阶段无法从结果中还原。外层清理随后独立确认 PostgreSQL Schema、Chroma Database、运行目录和日志目录均不存在；脱敏结果文件保留。故可确认没有残留隔离资源，但不能宣称运行器内层清理本身通过。
- 随后按 TDD 修复 FR-042 当前轮门控的 State 序列化适配：RED 用例复现序列化 human dict 被拒绝；GREEN 仅识别末条 `type="human"` 且字符串 `content`，允许无关元数据键并保持非用户/非文本消息不回退。定向测试 `20 passed`；后端全量 `580 passed, 2 skipped`；`compileall` 通过，主 Agent 另独立复跑定向测试与编译均通过。该修正不改变门控正则、补充查询或排序策略。真实答案/引用批次没有重跑；原 `3/10` 仍是当前唯一端到端质量结果，确定性门控通过不等于答案质量通过。本轮未追加 DeepSeek 请求，也未运行尚未授权的 Pixie 批次。
- 后续验收器指标拆分按 TDD 完成：先分别验证“引用确实映射但门控不匹配”和“映射失败但门控匹配”，拒答样本则将候选映射标为不适用；最终质量门仍独立检查适用的冻结证据/拒答条件、工具调用和门控。为留下 RED 证据，临时将映射恢复为旧合取后，映射通过但门控不匹配的用例按预期 `1 failed`（实际映射值被压成 `false`）；恢复独立映射计算后，定向测试 `23 passed`。定位链路是从旧结果中 `mapped_to_candidate=false` 与 `gate_matched=false` 同时出现开始，追到 `CandidateTrace.assess_citations()` 的合取表达式，再用固定候选和引用构造两种相反组合验证指标耦合。历史 JSON 未存引用摘录映射，故修复只改善后续观测，不追算旧批次。
- 验证期间的错误及排查：首次 `python -m pytest` 实际调用系统 Miniconda 解释器，报 `No module named pytest`；根据项目验证说明改用 Python 3.11 虚拟环境后测试运行。首轮定向测试的 traceback 指向质量汇总函数引用了未定义的 `is_refusal`，而局部变量名为 `is_refusal_case`；另一个拒答测试未为该请求登记工具轨迹，却断言 `tool_call_matched=true`。分别修正变量引用和测试前置轨迹后，定向测试 `23 passed`。后端全量 `586 passed, 2 skipped`，`compileall -q app tests scripts evals pixie_qa` 通过。未重跑真实模型，也未改变原 `3/10` 端到端结果。
- 后续清理诊断修正（确定性 TDD，未重跑模型）：**错误**是旧答案结果只含 `cleanup_verified=false` 和通用异常类型，无法指出内层失败步骤；**排查**从结果摘要追到 `run_answer_acceptance()` 的 `finally`，再追到共享 `_cleanup_and_verify()` 对逐步异常的合并；**已确认根因**是共享函数未保留步骤标识，答案摘要亦未写步骤码，而非已确认某个具体清理资源失败。先新增测试并实际运行，得到 `4 failed, 40 passed` 的 RED；随后最小修正为业务范围清理、API 关闭、Chroma 删除、本地目标删除和四层归零核验记录固定失败步骤码，多步失败仍逐项尝试，答案 JSON 仅写白名单代码，未知异常记 `UNKNOWN`，不写底层异常文本。定向 GREEN `44 passed`；后端全量 `590 passed, 2 skipped`；`compileall -q app tests scripts evals pixie_qa` 通过，主 Agent 独立复跑定向、全量及编译。**验证边界：**这些测试证明后续运行可记录清理阶段，不证明旧批次究竟失败在哪一步，也不改变旧 `3/10` 质量结果；本次未调用 DeepSeek、未重新访问隔离 PostgreSQL/Chroma。

### §5.4 Trace 与原始 PDF 离线语义复核

- 2026-09-26 只读取已有 Pixie Trace、离线候选/输出和本地固定资料 PDF，没有新模型调用、数据库写入或应用代码修改。核对原定 9 条扩展目标引用：WB-REV-LOAN-01 原问题重排 3 条、补充表达重排 3 条、WB-CONTEXT-NONLOAN-01 3 条，全部为 Belarus Snapshot 第 16 页且逐条映射到本题候选。原页表格列出 IBRD Loan Amount 250、Disbursed 0、Undisbursed 250，数据截至 2015-03-05；同页正文说明 Bruzgi 口岸预计处理能力从 700 提高至每日 1,700 辆。因此 9 条引用分别支持贷款金额或非贷款容量事实。
- 原问题贷款组另有 1 条额外引用至 Belarus Snapshot 第 7 页（仅 entry 2/sample 1）。PDF 原页将 M6 Transit Corridor Improvement 项目标为 US$250 million；该页支持项目金额，与第 16 页的 IBRD 表格互补，不计入原定 9 条目标页引用数。
- LUSHAN-03 的 entry 1/8/15 均回答 `Satisfactory` 并引用 `2024_completion_report_review.pdf` 第 14 页；原页明确写出项目 Overall Outcome 与 Outcome Rating 均为 Satisfactory。冻结目标第 20 页的 IEG Outcome 行也为 Satisfactory，且位于进入 Prompt 的第 5 位，但最终三次均引用第 14 页。因此语义回答/引用支持通过，严格冻结目标页仍 0/3；此项是目标页未命中，不是无依据回答。
- WB-REV-LOAN-01 补充表达组 entry 3/10/17 分别回答 `250百万美元`、`250 million US Dollars`、`2.5亿美元（250 million US Dollars）`。三者均表达 250 百万美元，第 16 页候选支持；现有 evaluator 只做 NFKC、大小写与空白归一后查找固定 `US$250 million` 子串，故评分 0/3 是格式契约的机械假阴性，不能据此说答案事实错误或排序导致退化。
- WB-UNSUPPORTED-DRAW-01 entry 6/13/20 都回答 250 million 并引用第 16 页候选。原表格显示的是截至 2015-03-05 的 `Amount=250`、`Disbursed=0`；问题询问当前或完工时实际提款额，证据没有该时间口径，正确行为应为固定拒答。检查 Trace 中模型确实收到完整表格列名、数值、日期及问题，并选择 `ANSWERED` / 250；因此可确认真实拒答/字段时间口径误答，不宜仅归结为召回缺失。
- 对这 3 条拒答样本，独立直接调用当前 `_citation_maps_to_candidate()` 均返回真，文件、PDF_PAGE 位置、摘录与第 16 页候选一致。总 evaluator 却给出 `CITATION_NOT_IN_CANDIDATES`：其拒答分支把 `citation_mapping_passed` 设为 `not citations`，将“拒答答案不应带引用”混作候选映射结果，错误码顺序又先报告映射失败。总分 0 仍正确，因为样本确实没有拒答且带引用；错误的是失败分类，不是候选映射真值。
- **本轮错误及排查链路：**（1）两次 PowerShell 汇总命令和一次 LUSHAN 条目提取因多写循环右花括号解析失败，没有执行写操作；改用分行循环/显式对象汇总后成功。（2）Poppler 报缺少 `nameToUnicode` 资源及 Symbol/ArialUnicode 字体，但仍生成第 7、16、14、20 页图像；逐页目视确认相关英文内容清晰可读，未影响语义结论。（3）首次 Python Trace 摘要在 PowerShell 代码页下出现中文乱码；改用 `python -X utf8` 后可正确显示，原 Trace 文件未损坏。
- **后续 9 次对照的候选日期复核更正：**首次预检只搜索 ISO 形式 `2015-03-05`，据此误判保存的第 16 页候选未包含日期。随后直接复核 `be69bcb22cd24b77/extension-candidates.json` 中三个 Belarus 条件的第 16 页候选，确认它们是同一段 2,532 字符摘录，包含表头、Amount/Disbursed/Undisbursed 数值，并以原文形式写有 `as of March 5, 2015`。误判根因是日期字符串形式假设过窄；检查原始候选上下文后纠正。三项对照可复用完全相同的真实候选，不需要补造或拼接日期证据。最多 9 次模型调用已获授权；代码实现授权状态见当前 handoff。
- **当前计划状态：**用户已确认执行[拒答边界与评测口径修正实施计划](../../implementation/FR039-FR042档案回答拒答边界与评测口径修正实施计划.md)，包含 TDD 代码实现和最多 9 次真实模型调用。阶段 A evaluator/样例契约 TDD、21 条离线重评与 Step 6 均已完成；阶段 B 共享 Prompt TDD 也已完成。阶段 C 的 9 次专用运行器与最终确定性全量回归待完成；通过前不得调用模型。未授权修改正式检索、公开 API 或冻结集。

### 拒答/金额评测器 TDD：阶段 A

- 按计划新增拒答映射“不适用”、金额等价表达组（组内 OR、组间 AND）和旧 `answer_fragments` 兼容断言。实现范围限于 `evals/archive/world_bank_evaluators.py`、`scripts/archive_world_bank_extension_eval.py` 与对应测试；未修改应用 Prompt、正式检索、API、冻结集或持久数据。
- TDD 记录：首轮目标用例 RED `13 failed, 52 passed`；新增分组/空列表边界后再次 RED `1 failed`；修正后主 Agent 使用 Python 3.11 虚拟环境独立复跑定向测试 `66 passed in 10.33s`。未调用真实模型或 Pixie。
- 21 条旧 Trace 离线重评分完成：基线 `12/21`，新结果 `15/21`；唯一改变分数的 `entry-3/10/17`（补充排序贷款金额）从 0/3 变为 3/3。无据提款 `entry-6/13/20` 仍 0/3，拒答映射改为不适用、错误码改为 `ANSWER_CONTRACT_FAILED`；LUSHAN-03 冻结页仍 0/3。原运行未覆盖，新运行目录为 `tests/pytest_docs/public_projects/world_bank_belarus_m6/acceptance/be69bcb22cd24b77/pixie-results/results/offline-rescore-20260926-cfaf0c8d/`；Step 6 分析与行动计划均已生成，官方验证器通过。全程未重放模型、未检索、未写数据库或向量库。
- 后续三组对照可复用的 8 条 Belarus Snapshot 候选已在旧扩展输入中核对：`WB-UNSUPPORTED-DRAW-01` 的三份 `archive_question_retrieval` 负载一致，规范化 JSON SHA-256 均为 `c6f9169f32a66c879aa791e5d9fee331bf69152dd73c62c326f5f42d3c27e4d0`，均包含第 16 页。实际九条目须从同一负载复制，避免条件间候选差异。
- **错误及定位链路：**RED traceback 中中文乱码。先观察到异常输出仅在 PowerShell 终端显示异常；随后核对 `chcp` 得到活动代码页 936、PowerShell Console/管道输出编码均为 UTF-8，再运行 Python 编码探针确认子进程 stdout 为 GBK。根因是子进程输出字节编码与 PowerShell 解码编码不一致，未涉及源码、评测断言或测试结果；测试进程仍给出有效退出状态。后续测试运行显式令 Python 使用 UTF-8 并与终端输出编码保持一致，不通过修改业务代码处理显示问题。

### 共享档案 Prompt TDD：阶段 B

- 在 `_build_archive_prompt` 中增加通用表格规则：答案字段、单位/币种和统计日期必须匹配问题；`Amount`、`Disbursed`、`Undisbursed` 不可互换；历史 `as-of` 值不能证明当前或完工时值，证据缺少对应时间口径时拒答。无项目名、金额或 Belarus 特例；JSON、引用与既有直接证据规则保持原样。
- 新增 Prompt 契约测试先 RED `1 failed, 26 deselected`，失败点为新增表格规则不存在；最小 Prompt 修改后该测试文件 GREEN `27 passed`。输出有 72 条现存 Pixie instrumentation/jsonpickle 弃用警告，测试退出结果通过；未发生需追加定位的测试错误。
- 本阶段只验证规则进入共享提示，不证明模型会遵守；没有调用真实模型或 Pixie。完整后端测试和编译尚待运行器代码完成后统一执行一次。

### 阶段 C：九题真实模型对照执行记录

- **运行前确定性门：**最终后端全量测试 `707 passed, 2 skipped, 226 warnings`；`python -m compileall -q app tests scripts evals pixie_qa` 与 `git diff --check` 通过。专用数据集位于 Git 忽略目录 `tests/pytest_docs/public_projects/world_bank_belarus_m6/acceptance/focused-m6-20260926-dbb64c4149082b/focused-world-bank-m6-20260926.json`，共 9 条（三个条件各 3 条），均使用 `WorldBankFocusedQuestionRunnable` 的九次硬上限和零客户端重试。
- 三种问题的 `input_data` 都只有用户问题；所有条目注入完全相同的 8 条 `archive_question_retrieval` 候选。按既有规范化方式核对完整负载 SHA-256 为 `c6f9169f32a66c879aa791e5d9fee331bf69152dd73c62c326f5f42d3c27e4d0`，并含 Belarus Snapshot 第 16 页。冻结目标均为 `null`；第 16 页只登记为扩展来源诊断，不并入冻结集评分。未重新检索，也未写 PostgreSQL、Chroma 或业务数据。
- Pixie 使用专用 `PIXIE_ROOT`，已启动本地 UI（端口 7120）。显式运行该单一数据集后，命令以退出码 1 结束：输出含可选 provider 依赖冲突提示、两条 `archive_question_judgment_failed` 安全日志，以及最终 `RUNNABLE_FAILED`。结果目录存在但没有任何逐条结果/Trace 文件，因此没有可评分的九题输出，也未生成成功运行所需的 Step 6 分析和验证工件。
- **安全排查链路：**从 CLI 异常定位到 Pixie `run_dataset()` 对同一个 Runnable 实例并发调度九条输入；Runnable 内部信号量串行化调用并共享预算。沿 `WorldBankFocusedQuestionRunnable.run()` 的脱敏异常分支到 `answer_archive_question()`，再到 `judge_archive_answer()`：模型调用异常和严格 JSON 解析异常都被统一包装为 `ArchiveAnswerJudgmentError`，业务服务只输出固定事件 `archive_question_judgment_failed` 并返回通用不可用错误，Runnable 再归一为 `RUNNABLE_FAILED`。现有安全输出不能区分调用方/传输故障与模型响应契约错误；Pixie 未落盘 Trace，预算计数器也未持久化，所以只能确认观察到两次裁决失败，不能据此断言精确实际调用总数。上限仍由同一 Runnable 实例硬性限制为最多 9，自动重试为 0。
- 未读取 `.env`、运行日志或任何密钥，也未用额外探测/重试去猜测底层异常。Pixie 的可选 provider 依赖提示不足以证明根因。当前没有证据表明应用代码本身有缺陷，因此不新增错误修复文档、不追加代码改动；更不能宣称提示修正通过真实语义验收。依据计划停止本轮，剩余预算不再使用；若继续真实评测，需另行确认新的调用预算和方案。

### 阶段 C 第二次运行：拒答边界真实对照与运行器诊断

- **授权与运行边界：**用户对首次失败后重新确认了最多 9 次真实 DeepSeek 请求，限定三个条件各 3 次、客户端重试关闭、首个运行故障熔断、不补跑。本次使用新的隔离 `PIXIE_ROOT` 和已保存的专用九条数据集；不重新检索，候选通过 Pixie 输入注入，未访问或写入 PostgreSQL、Chroma、Checkpoint 或原文件。运行前确认凭据由应用 `.env` 配置加载（未输出值）、数据集为 9 条且三组各 3 条，每条问题输入仅含 `question`，每条注入同一 8 项检索结果；九份完整检索输入逐项等于保存的扩展候选文件中的原始输入，均含 Belarus Snapshot 第 16 页。模型实际提示中可见表格字段、数值、日期和新增时间口径规则。
- **真实响应与评分：**9 条问题均产生最终问答响应；每条均为 `REFUSED_NO_EVIDENCE` 且零引用。通过保存的 Pixie Trace 以同一个确定性 `world_bank_answer_evaluator` 离线重建评分，得到：`WB-M6-IBRD-AMOUNT-01` 为 `0/3`，`WB-M6-DISBURSED-AS-OF-01` 为 `0/3`，`WB-M6-CURRENT-DISBURSED-REFUSAL-01` 为 `3/3`，合计 `3/9`。前两组预期有据回答，却全部被拒答（评测原因 `MISSING_CITATION`）；当前/完工提款组预期拒答，三条均满足拒答且无引用。该离线重评分只说明已保存响应的确定性契约结果，不是成功 Pixie 运行或完整 Step 6。
- **证据与定位：**九条 Trace 的 `archive_question_decision` 均为 `has_evidence=true`、8 个候选；`archive_question_prompt_evidence` 均包含第 16 页，摘录可见 `Amount`、`Disbursed`、`Undisbursed`、250、0 和 `as of March 5, 2015`，新增表格时间规则也进入模型提示。九条 `archive_evidence_sufficiency_decision` 均记录 `sufficient=false` 且未选引用，最终响应均为固定拒答。代码核对确认这不是独立的“充分性分类器→回答生成器”两阶段流程：`judge_archive_answer()` 只调用一次 `model.invoke()`，同一个严格结构化模型输出同时决定回答/拒答和引用；该 sufficiency Wrap 是从此决策派生的事后观测。因此，本批六条有据问题的直接失败表现是单次结构化决策过度拒答，没有输出错误金额；拒答输出本身不包含模型理由，无法判定它是误读字段/日期还是采取了过度保守策略。不得把“新增规则导致过度保守”写成已证实根因。
- **Pixie 运行状态：**命令退出码为 1。隔离结果根中有九份逐题 `trace.jsonl`，但没有逐题 `eval-output.jsonl` 或 `evaluations.jsonl`；因此原生 Pixie 评分和 Step 6 工件未生成。随后只读检查 Pixie 安装源码与隔离结果目录：CLI 已为九条写出 Trace，结果保存流程也已写出运行元数据、数据集元数据和第 0 条 `config.json`，但第 0 条 `eval-input.jsonl` 尚不存在；由保存函数的写入顺序可确认流程未完成，停止点在首条配置之后、评测输入文件完成之前。具体异常/中断原因无法从这些工件确定，未读取运行日志，故不推断为序列化、文件系统或评测器缺陷。每题的 LLM Trace 含两条消息与 Token 计数完全相同的重复 span 记录，不能据此计为两次请求；生产判定服务每题调用一次 `model.invoke`，客户端重试为 0，本批最多 9 次且没有补跑。
- **已确认的运行器观测缺陷及修复：**对照运行器源码与 Trace 后发现，`WorldBankQuestionRunnable._run_focused()` 仅在预算异常和一般异常分支调用 `_emit_execution_event()`；正常 `COMPLETED` 路径只更新内存预算事件，却没有将安全尝试数写入 Pixie。故成功 Trace 缺少 `archive_world_bank_execution`，不能单靠该批产物审计逐题预算。按 TDD 新增成功路径计数事件测试，RED `1 failed`（观察列表为空）；最小修复是在成功 `else` 分支发出事件，定向测试 `1 passed`，运行器模块 `16 passed, 4 warnings`。该缺陷解释成功计数事件缺失，不解释 Pixie CLI 退出或结果文件缺失，也不改变已经产生的九条模型响应。
- **最终确定性验证：**成功事件修复后，后端全量测试 `720 passed, 2 skipped, 231 warnings`；`python -m compileall -q app tests scripts evals pixie_qa` 通过。
- **结论与停止点：**Amount 与指定日期 Disbursed 的回答边界未解决，质量门失败；当前/完工无据拒答通过。九次真实调用预算已全部使用，不再调用或补跑。Pixie CLI 持久化故障原因待查，但不得为查明原因读取运行日志。任何进一步 Prompt/应用行为修改先回到方案评审；如需新真实模型验证，另行制定并授权预算。由于本次 CLI 未成功生成标准评分目录，本节离线重评分不冒充 Step 6；正式 Step 6 尚未完成。

### 阶段 D：时间口径正向澄清与判定失败安全分类（2026-09-26）

- **触发证据与根因边界：**阶段 C 的九条真实输出均为拒答；有据的批准金额与指定日期提款额两组为 `0/3`，当前/完工提款组为 `3/3`。候选和完整表格字段、单位、日期都已进入模型提示，但输出没有拒答理由。因此只能确认现象是有据问题过度拒答，不能证明是哪条提示规则或推理步骤导致。阶段 D 把“未明确说明精确匹配的历史 as-of 问题可回答”作为待验证假设，不写成已证实根因。
- **提示修正：**共享档案 Prompt 现明确：问题询问截至日期，且同一字段证据标注同一日期时，该历史值支持该历史问题，回答应注明日期；若问题问当前或完工时而证据只有更早日期，仍必须拒答。字段、单位/币种及 `Amount` / `Disbursed` / `Undisbursed` 区分规则保留。不含项目名、固定金额或 Belarus 专属逻辑。
- **错误分类与安全边界：**`ArchiveAnswerJudgmentError` 增加白名单 `failure_kind`：`PROMPT_INPUT_INVALID`、`MODEL_CALL_FAILED`、`MODEL_OUTPUT_INVALID`、`UNKNOWN`。判定输入构造、模型调用、响应解析分别在各自边界分类；只有原有连接/超时模型调用类别可重试。Pixie state Wrap 与 FR-039 安全日志只记录类别，不包含异常正文或模型原始响应；FR-039 仍返回原 503 契约。真实 FR-042 Graph 失败路径的 Wrap 行为由共享判定函数承接，但尚未做真实 Graph/Pixie 运行验证。
- **TDD 与确定性验证：**精确历史日期提示测试 RED `1 failed`，补充规则后通过；三类异常分类及既有 503/日志安全测试 RED `4 failed`，实现后定向 `5 passed`。问答服务、FR-042 Graph、应用执行及评测运行器相关测试 `96 passed`；最终后端全量 `725 passed, 2 skipped`，`compileall -q app tests scripts evals pixie_qa` 通过。未增加模型调用。
- **结论：**这轮只证明 Prompt 契约和安全故障分类实现符合确定性测试；不证明模型不再过度拒答，也不等于 Pixie Step 6 完成。九次模型预算已用完。重新评测需先通过隔离 PostgreSQL/Chroma 预检，并另行获得明确的新调用上限；见[拒答边界修正实施计划阶段 D](../../implementation/FR039-FR042档案回答拒答边界与评测口径修正实施计划.md)。

### 阶段 D 后真实模型复验（2026-09-26）

- **授权与固定输入：**用户另行授权最多 9 次真实模型 HTTP 尝试，三项条件各 3 次，首错熔断且不补跑。使用更新后的共享 Prompt、生产 FR-039 回答服务、同一份固定 Belarus 第 16 页来源候选；不重新检索或访问 PostgreSQL/Chroma。运行前逐项确认 9 条数据、三组各 3 条、每条问题输入只含 `question`，并注入相同的 8 条候选且包含第 16 页。Pixie 根隔离在 `tests/pytest_docs/public_projects/world_bank_belarus_m6/acceptance/answer-boundary-stage-d-20260926/pixie/`。
- **模型调用结果：**首条在 `model.invoke()` 阶段发生异常，FR-039 安全日志及失败分类只暴露 `MODEL_CALL_FAILED`。调用预算器在模型委托前计数为 1，生产客户端 `max_retries=0`；运行器首错熔断，不为后续样本发起调用。是否有 HTTP 请求到达 DeepSeek 服务端无法由当前白名单观测证明，故报告“1 次模型调用尝试”，不声称服务端已收到 1 个请求。授权剩余次数不再使用。
- **Pixie 工件与运行故障：**Pixie 已为九个条目建立逐条 Trace 路径，但在打印 `✗` 时 PowerShell/Python GBK 输出编码触发 `UnicodeEncodeError`。核对 Pixie CLI 安装源码的顺序可见，逐条终端摘要打印发生在 `save_test_result()` 之前；本次因此没有标准 RunResult、evaluation 输出或评分文件，也没有可供验证的 Step 6。未读取 `.env`、运行日志或 Trace 正文；可选 provider 依赖提示不构成根因。
- **失败原因与排查边界：**已确认生产模型调用边界失败类别为 `MODEL_CALL_FAILED`，但没有获准读取的安全细节可区分网络、服务端/API 或其他底层原因；不推断根因。另一个 `UnicodeEncodeError` 是本地命令行展示编码问题，不能作为项目功能缺陷或模型质量结果。按首错熔断规则不重跑、不用剩余预算补样，也不新增项目缺陷 RCA。
- **结论与下一步：**本轮没有三组真实质量分数，不能验证阶段 D 的提示澄清效果，也不能将历史 `3/9` 变更为新质量结果。若继续，须先基于不涉及原始日志/秘密的可用诊断确认模型调用失败类别能否缩窄，并为任何新的真实请求重新确认预算；Pixie 终端编码应在不调用模型的本地 smoke 检查中先行修正。FR-042 Graph→真实 Chroma 联调仍需单独通过 PostgreSQL 隔离预检。

### §5.2 检索诊断修复后的隔离复验（2026-09-26）

- **范围与运行：**运行 `scripts.archive_world_bank_extension_eval`，本轮 ID `6f2c6ee6e9894b44`。隔离索引包含 P153548 四份固定 PDF 和 Belarus Snapshot 一份扩展 PDF；来源哈希校验通过。九个检索诊断用例完成，并从其中七个候选条件各生成三条、共 21 条 Pixie 候选记录。检索与本地 Embedding/CrossEncoder 均真实运行；FR-042 使用脚本模型驱动的 Graph 探针，`external_model_calls=0`，未调用 DeepSeek。固定评测集、正式配置、检索实现和公开 API 均未修改。
- **LUSHAN-01：**原始与补充 dense 路各取 30 条，交集 6、并集 54。冻结目标的公开证据覆盖在原问题重排第 21、`IBRD IDA` 重排第 3；精确 Chunk 摘录匹配两路均未命中。同次记录的五个已人工核实金额支持页，在原问题 / 补充表达重排中的名次依次为：2016 appraisal 第 1 页 `2/9`、第 16 页 `21/3`；2022 restructuring 第 4 页 `4/7`；2024 completion report 第 1 页 `1/8`；completion review 第 1 页 `33/1`。Top-8 覆盖由原问题路 `3/5` 变为补充路 `4/5`，只作页面诊断，不作完整召回率或冻结门得分。
- **Belarus `WB-REV-LOAN-01`：**两路各取 20 条，交集/并集均为 `20/20`。第 7 页项目背景在原问题 / 补充表达重排分别为第 `2/17`；第 16 页 IBRD 贷款明细分别为第 `1/7`。因此补充表达保留金额证据在 Top-8，但会把背景页排出 Top-8；页面角色不混同。该来源同时包含 World Bank 与 IBRD，不是严格反向措辞正例。
- **门控与其余冻结题：**`LUSHAN-02/03/04` 均只检索原问题，公开支持页排名为 `1/5/无`；非贷款 `WB-CONTEXT-NONLOAN-01`、实体负例 `WB-ENTITY-NEG-01` 与未门控无据题 `WB-UNSUPPORTED-DRAW-01` 均未走补充 dense 路。`WB-UNSUPPORTED-DRAW-GATED-01` 触发补充路，但支持答案的候选数仍为 0；第 16 页只有相关上下文，补充表达下排名第 7。它证明该次检索没有提供“完工时提款额”支持，不证明真实回答模型会拒答。
- **FR-042 Graph 探针：**脚本化 Graph 完成 1 次工具调用，Top-8 含 LUSHAN-01 目标证据（第 3）；没有外部模型调用。此项只验证当前问题经 Graph 到正式检索工具的候选链路，不验证答案质量或引用卡片序列化。
- **延迟口径：**单次观测，不是重复采样或中位数。LUSHAN-01 生产检索服务 `11,048.705 ms`；同候选并集的离线原问题 / 补充表达重排分别 `12,666.185 / 11,282.803 ms`。Belarus 对应生产服务 `5,104.603 ms`，离线两种重排分别 `4,936.253 / 4,938.462 ms`。离线双排序与生产单次排序分开计时；这些单次值不用于跨资料池的延迟比较或稳定预算推断。
- **安全摘要与清理：**忽略目录中的 `tests/pytest_docs/public_projects/world_bank_belarus_m6/acceptance/6f2c6ee6e9894b44/summary.json` 保存白名单诊断：不含原问题、摘录、原始 Chunk/Document ID 或异常正文。运行器报告 `cleanup_verified=true`；随后用专用目标名独立只读复核 PostgreSQL Schema、Chroma Database、Checkpoint/运行目录及临时日志均不存在，复核结果 `true`。21 条候选数据与脱敏摘要留在忽略目录供获准的后续评测使用。
- **结论：**本次真实索引检索与 Graph→工具→检索证据链完成，结果支持候选层排序诊断；它不解决 Amount/Disbursed 与 as-of 日期口径的真实回答行为，不证明拒答、事实性或引用质量通过，也不授权选择融合策略。

#### 本阶段错误修复与定位记录

| 现象 | 根因及定位链路 | 修复与验证 |
| --- | --- | --- |
| Graph 检索探针结束后仍有 12 条 Checkpoint 消息 | 探针创建随机 `thread_id`，但不创建 `AgentSession`；项目范围清理无法发现这条孤立线程。检查 Checkpoint 行数后回溯探针生命周期，确认线程没有删除步骤。 | 在探针 `finally` 中调用 runtime 的 `delete_thread(thread_id)`；增加 Checkpoint 归零断言，测试先观察到 12 条残留（RED），修复后定向通过。 |
| 本地 Checkpoint 的 WAL/SHM 文件占用，精确清理拒绝删除 | `_checkpoint_row_count()` 使用 SQLite connection context manager，但它不保证显式关闭连接；LangGraph `SqliteSaver.setup()` 将数据库设为 WAL，探针连接关闭不完整时 sidecar 仍被占用。沿行数探针和目录清理路径定位到连接生命周期。 | 显式关闭只读计数连接；删除前验证行数为零、执行 WAL checkpoint/truncate 并检查 busy 状态，再关闭连接、移除 sidecar 和精确 Checkpoint 文件。新建 WAL 测试先复现拒删，修正后通过。 |
| 内层报告 Chroma 已清理，外层又因删除不存在的 Database 把成功标为失败 | `_cleanup_and_verify()` 已删除 Chroma 并验证四层归零；协调器随后重复执行 `delete_chroma_database`，将已成功清理误报为失败。对照内外清理调用顺序及独立资源核验后确认。 | 内层清理成功时跳过外层重复删除；删除助手对不存在的专用 Database 先做存在性检查，保持幂等。补充调用顺序及缺失 Database 测试，RED 后 GREEN。 |
| 有效的第九个固定检索用例导致安全摘要整体 `RETRIEVAL_FAILED` | 计划生成 4 个 LUSHAN 与 5 个 Belarus 用例；脱敏器白名单只有 8 个 ID，缺少 `WB-UNSUPPORTED-DRAW-GATED-01`。对照 `build_query_plan()` 与白名单后定位。 | 将该固定 ID 纳入白名单；保持未知 case fail-closed。协调器测试先复现摘要被拒（RED），修正后通过。 |
| 完整真实检索完成后摘要阶段抛 `AttributeError`，没有生成候选记录 | 安全失败阶段细化为 `prepare.sanitize_retrieval_diagnostics`，白名单诊断依次确认 `AttributeError`、脱敏器源码第 1115 行。该行访问 `value.get("fr042_graph_probe")`；上游 `ranking_summary` 列表推导式中的海象变量 `value := rank(item)` 覆盖了函数参数 `value`，使其变成整数。最小摘要测试以非空相关排名和 Graph 探针稳定复现（RED）。 | 把两个列表推导式的临时排名变量改为 `clean_rank`，不改摘要契约；回归断言同时检查排名汇总和 Graph 探针均保留。相关验收模块 `103 passed, 2 warnings`，`compileall` 与 `git diff --check` 通过；后续隔离真实运行生成 21 条记录并清理成功。 |

运行中曾有一次失败仅定位到 `prepare.document_seeding`，其安全摘要没有保留操作子步骤；未读取日志，也没有证据确认项目缺陷或其根因，因此不将其写成已修复的 RCA，也不把后续成功误称为该未知原因的修复证明。

- **最终验证：**相关检索验收模块 `103 passed, 2 warnings`；后端全量 `732 passed, 2 skipped, 241 warnings`，退出码 0；`python -m compileall -q app tests scripts evals pixie_qa` 通过。全量测试进程未设置仅供 PostgreSQL 集成测试使用的 `POSTGRES_TEST_URL` 与 `RUN_POSTGRES_CONCURRENCY_TEST`；两个对应集成测试跳过。`git diff --check` 通过，只有既有 LF/CRLF 转换提示。
